# 🔧 Руководство по отладочной панели раскосов

## 📋 Описание

Отладочная панель позволяет настраивать расположение раскосов относительно столбов и мауэрлатов в реальном времени.

## 🎯 Как использовать

1. **Откройте страницу калькулятора** - панель отладки автоматически появится рядом с 3D окном
2. **Настройте параметры** - используйте слайдеры и поля ввода для изменения позиции раскосов
3. **Наблюдайте изменения** - 3D модель обновляется автоматически с задержкой 150мс

## ⚙️ Параметры настройки

### Смещения (м)
- **dX (вост-зап)** - смещение по оси X (восток-запад)
- **dY (высота)** - смещение по оси Y (высота)
- **dZ (юг-сев)** - смещение по оси Z (юг-север)

### Повороты (°)
- **Rot X** - поворот вокруг оси X (в градусах)
- **Rot Y** - поворот вокруг оси Y (в градусах)
- **Rot Z** - поворот вокруг оси Z (в градусах)

### Позиционирование (м) - НОВОЕ!
- **X (от столба)** - смещение по X относительно столба
- **Y (высота)** - смещение по Y относительно мауэрлата
- **Z (длина)** - смещение по Z вдоль длины конструкции
- **Начало (м)** - начало раскоса относительно мауэрлата (отрицательное = ниже)
- **Конец (м)** - конец раскоса относительно мауэрлата (0 = нижняя плоскость мауэрлата)
- **Длина Z (м)** - длина раскоса вдоль оси Z

## 📐 Примеры использования

### Настройка высоты раскосов
1. Установите **Начало (м)** = -0.3 (300 мм ниже мауэрлата)
2. Установите **Конец (м)** = 0 (на уровне нижней плоскости мауэрлата)
3. Наблюдайте изменения в 3D модели

### Смещение раскосов от столба
1. Установите **X (от столба)** = 0.05 (50 мм от столба)
2. Раскосы сдвинутся дальше от столба

### Изменение длины раскосов
1. Установите **Длина Z (м)** = 0.5 (500 мм)
2. Раскосы станут длиннее вдоль оси Z

## 🔄 Сброс настроек

Нажмите кнопку **"Сбросить"** в секции левого или правого раскоса для возврата к значениям по умолчанию.

## ✅ Включение/отключение

Используйте чекбокс **"Применять смещения"** для включения/отключения всех отладочных параметров.

## 📝 Примечания

- Все значения в метрах (1 м = 1000 мм)
- Повороты в градусах
- Изменения применяются ко всем раскосам соответствующей стороны (левый/правый)
- 3D модель обновляется автоматически с задержкой 150мс для оптимизации производительности

---

**Создано:** 2025-01-XX  
**Версия:** 1.0



























































