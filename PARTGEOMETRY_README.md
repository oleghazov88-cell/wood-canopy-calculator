# Part Geometry - GLB to Three.js Converter

## 📋 Описание

Этот проект содержит точную программную копию 3D геометрии из GLB файла, созданную в чистом Three.js коде **БЕЗ загрузки GLB во время выполнения**.

## 📁 Файлы

### Основной файл
- **`naves-calc/assets/js/partGeometry.js`** - Готовая к использованию функция, возвращающая Three.js меш

### Утилита для извлечения
- **`extract_glb.html`** - HTML-утилита для извлечения геометрии из GLB файлов в браузере
- **`simple_extract.ps1`** - PowerShell скрипт для извлечения геометрии (использовался для создания partGeometry.js)

## 🎯 Характеристики извлеченной модели

**Источник:** `raskos/r2.glb`

**Геометрия:**
- Вершины: **162**
- Треугольники: **100** (300 индексов)
- Атрибуты: Position, Normal
- Тип индексов: Uint32Array

**Материал:**
- Тип: MeshStandardMaterial
- Цвет: 0x8B4513 (коричневый - дерево)
- Roughness: 0.8
- Metalness: 0.0

## 🚀 Использование

### Базовый пример

```javascript
import * as THREE from 'three';
import { createPartMesh } from './assets/js/partGeometry.js';

// Создание сцены
const scene = new THREE.Scene();
const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
const renderer = new THREE.WebGLRenderer();

renderer.setSize(window.innerWidth, window.innerHeight);
document.body.appendChild(renderer.domElement);

// Добавление меша из partGeometry.js
const mesh = createPartMesh();
scene.add(mesh);

// Освещение
const light = new THREE.DirectionalLight(0xffffff, 1);
light.position.set(5, 5, 5);
scene.add(light);

const ambientLight = new THREE.AmbientLight(0x404040);
scene.add(ambientLight);

// Камера
camera.position.z = 20;

// Анимация
function animate() {
    requestAnimationFrame(animate);
    mesh.rotation.y += 0.01;
    renderer.render(scene, camera);
}

animate();
```

### Модификация меша

```javascript
const mesh = createPartMesh();

// Изменение позиции
mesh.position.set(10, 5, 0);

// Изменение масштаба
mesh.scale.set(2, 2, 2);

// Изменение материала
mesh.material.color.set(0xFF5733);
mesh.material.roughness = 0.5;
mesh.material.metalness = 0.3;

scene.add(mesh);
```

### Использование в существующем проекте

Если вы используете этот меш в проекте `naves-calc`, можно импортировать его так:

```javascript
import { createPartMesh } from './assets/js/partGeometry.js';

// В вашем 3D рендерере
class Canopy3DRenderer {
    addCustomPart() {
        const part = createPartMesh();
        
        // Позиционирование относительно навеса
        part.position.set(x, y, z);
        part.rotation.set(rx, ry, rz);
        
        this.scene.add(part);
    }
}
```

## 🛠️ Извлечение геометрии из других GLB файлов

### Вариант 1: HTML-утилита (рекомендуется)

1. Откройте `extract_glb.html` в браузере
2. Выберите GLB файл (например, `raskos/r1.glb`, `r3.glb`, или `r4.glb`)
3. Нажмите "Извлечь геометрию"
4. Скопируйте сгенерированный код
5. Сохраните в новый JS файл

### Вариант 2: PowerShell скрипт

```powershell
# Для извлечения другой модели
powershell -ExecutionPolicy Bypass -File simple_extract.ps1 -GlbPath "raskos/r3.glb" -OutputPath "naves-calc/assets/js/part3Geometry.js"
```

**Доступные модели в проекте:**
- `raskos/r1.glb` - Раскос тип 1 (самый простой, 2KB)
- `raskos/r2.glb` - Раскос тип 2 (крестообразный, 6KB) ✓ **используется**
- `raskos/r3.glb` - Раскос тип 3 (двойной, 7KB)
- `raskos/r4.glb` - Раскос тип 4 (угловой, 16KB)

## 📊 Преимущества этого подхода

✅ **Без зависимости от GLTFLoader** - не нужно загружать дополнительные библиотеки  
✅ **Мгновенная загрузка** - геометрия создается синхронно, без асинхронных операций  
✅ **Полный контроль** - можно легко модифицировать геометрию или материал  
✅ **Меньше HTTP запросов** - не нужно загружать GLB файлы отдельно  
✅ **Работает оффлайн** - не требует доступа к файловой системе  

## ⚠️ Недостатки

❌ **Больший размер JS файла** - геометрия включена в код (16KB для r2.glb)  
❌ **Сложнее обновлять** - при изменении 3D модели нужно перегенерировать код  
❌ **Не подходит для сложных моделей** - модели с тысячами вершин создадут огромный JS файл  

## 💡 Рекомендации

- **Используйте этот подход** для простых, часто используемых элементов (раскосы, столбы)
- **Используйте GLTFLoader** для больших, редко используемых моделей
- **Комбинируйте оба подхода** в одном проекте для оптимальной производительности

## 🔧 Технические детали

### Структура createPartMesh()

Функция `createPartMesh()` возвращает готовый `THREE.Mesh` со следующей структурой:

```javascript
{
    geometry: THREE.BufferGeometry {
        attributes: {
            position: Float32Array[486],  // 162 вершины * 3
            normal: Float32Array[486]      // 162 нормали * 3
        },
        index: Uint32Array[300]            // 100 треугольников * 3
    },
    material: THREE.MeshStandardMaterial {
        color: 0x8B4513,
        roughness: 0.8,
        metalness: 0.0
    }
}
```

### Формат данных

- **Position** - координаты вершин в формате (x, y, z), Float32Array
- **Normal** - нормали для освещения, Float32Array
- **Indices** - индексы вершин для формирования треугольников, Uint32Array

### Координатная система

Модель использует стандартную координатную систему Three.js:
- **X** - слева направо
- **Y** - снизу вверх
- **Z** - от экрана к наблюдателю

## 📝 История создания

Создано с помощью PowerShell скрипта `simple_extract.ps1`, который:
1. Читает бинарный GLB файл
2. Парсит JSON структуру (glTF 2.0)
3. Извлекает бинарные данные геометрии
4. Генерирует чистый JavaScript код

**Дата создания:** 2025-11-20  
**Версия glTF:** 2.0  
**Генератор исходного GLB:** SimLab GLTF  

---

**Примечание:** Этот файл был создан автоматически. При необходимости обновления 3D модели, перегенерируйте код с помощью утилит, указанных выше.





