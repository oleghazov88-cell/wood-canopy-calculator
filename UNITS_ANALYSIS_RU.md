# 📐 Единицы измерения в раскосах

## 🔍 Анализ координат

### Примеры координат из GLB файлов:

**R1 (простой раскос):**
```
X: 3.937008
Y: 15.748030
Z: 19.685040
```

**R2 (крестообразный):**
```
X: 3.937008
Y: 15.748030
Z: 15.748030
```

---

## 🎯 Определение единиц измерения

### Вариант 1: ДЮЙМЫ (inches) ✅

Проверим, если это дюймы (1 inch = 25.4 mm):

```
3.937008 inches × 25.4 = 100.00 mm = 10.0 cm  ✓
15.748030 inches × 25.4 = 400.00 mm = 40.0 cm  ✓
19.685040 inches × 25.4 = 500.00 mm = 50.0 cm  ✓
```

**Результат:** Идеально! Все числа дают круглые значения в миллиметрах!

### Вариант 2: Сантиметры (см) ❌

```
3.937008 см = 39.37 mm  ❌ (странное число)
15.748030 см = 157.48 mm  ❌ (не круглое)
19.685040 см = 196.85 mm  ❌ (не круглое)
```

**Результат:** Не подходит

### Вариант 3: Миллиметры (мм) ❌

```
3.937008 мм = 0.39 см  ❌ (слишком маленький раскос)
15.748030 мм = 1.57 см  ❌ (нереально маленький)
```

**Результат:** Не подходит

---

## ✅ ВЫВОД: Единицы измерения = **ДЮЙМЫ (inches)**

Все координаты в файлах `partGeometry_r*.js` указаны в **дюймах**.

---

## 📊 Реальные размеры раскосов

### R1 (простой раскос)

| Координата | Дюймы | Миллиметры | Сантиметры |
|------------|-------|------------|------------|
| **Ширина (X)** | 3.937 | 100 mm | 10 cm |
| **Высота (Y)** | 19.685 | 500 mm | 50 cm |
| **Глубина (Z)** | 19.685 | 500 mm | 50 cm |

**Габариты:** 10 × 50 × 50 см (ширина × высота × глубина)

### R2 (крестообразный раскос)

| Координата | Дюймы | Миллиметры | Сантиметры |
|------------|-------|------------|------------|
| **Ширина (X)** | 3.937 | 100 mm | 10 cm |
| **Высота (Y)** | 15.748 | 400 mm | 40 cm |
| **Глубина (Z)** | 15.748 | 400 mm | 40 cm |

**Габариты:** 10 × 40 × 40 см

### R3 (двойной раскос)

Аналогичные размеры с R2: ~10 × 40 × 40 см

### R4 (угловой раскос)

Более сложная геометрия, но в тех же пределах: ~10 × 50 × 50 см

---

## 🔄 Конвертация единиц

### Из дюймов в миллиметры:

```javascript
const inchesToMm = (inches) => inches * 25.4;

// Примеры
inchesToMm(3.937008);  // = 100 mm
inchesToMm(15.748030); // = 400 mm
inchesToMm(19.685040); // = 500 mm
```

### Из миллиметров в дюймы:

```javascript
const mmToInches = (mm) => mm / 25.4;

// Примеры
mmToInches(100); // = 3.937008 inches
mmToInches(400); // = 15.748031 inches
mmToInches(500); // = 19.685039 inches
```

---

## 🛠️ Масштабирование раскосов

Если нужно изменить размер в Three.js:

### Метод 1: Масштабирование меша

```javascript
import { createBraceR2 } from './partGeometry_r2.js';

const mesh = createBraceR2();

// Увеличить в 2 раза (будет 20 × 80 × 80 см)
mesh.scale.set(2, 2, 2);

// Только по высоте (10 × 80 × 40 см)
mesh.scale.set(1, 2, 1);

scene.add(mesh);
```

### Метод 2: Конвертация в метры (для работы в метрах)

```javascript
const mesh = createBraceR2();

// Конвертируем из дюймов в метры
// 1 inch = 0.0254 м
const inchesToMeters = 0.0254;
mesh.scale.set(inchesToMeters, inchesToMeters, inchesToMeters);

// Теперь размеры в метрах: 0.1 × 0.4 × 0.4 м
scene.add(mesh);
```

### Метод 3: Конвертация в сантиметры

```javascript
const mesh = createBraceR2();

// Конвертируем из дюймов в см
// 1 inch = 2.54 см
mesh.scale.set(2.54, 2.54, 2.54);

// Теперь размеры в см: 10 × 40 × 40 см
scene.add(mesh);
```

---

## 📏 Почему дюймы?

GLB файлы были экспортированы из **SketchUp**, который часто использует дюймы как единицы измерения по умолчанию (особенно в американских настройках).

### Признаки использования дюймов:

1. ✅ Числа типа 3.937008 (точно 100 мм)
2. ✅ Числа типа 15.748030 (точно 400 мм)
3. ✅ Все размеры дают круглые значения в миллиметрах
4. ✅ Метаданные GLB: `"generator": "SimLab GLTF"` (часто использует дюймы)

---

## 🎯 Рекомендации

### Для работы в проекте naves-calc:

**Вариант 1: Оставить как есть (дюймы)**
- ✅ Не нужно ничего менять
- ✅ Работает как в оригинальных GLB
- ⚠️ Нужно помнить, что 1 единица = 1 дюйм

**Вариант 2: Масштабировать в метры**
```javascript
const brace = createBraceR2();
brace.scale.setScalar(0.0254); // дюймы → метры
scene.add(brace);
```

**Вариант 3: Масштабировать в сантиметры**
```javascript
const brace = createBraceR2();
brace.scale.setScalar(2.54); // дюймы → см
scene.add(brace);
```

### Для новых раскосов:

При создании новых раскосов или модификации существующих учитывайте:
- **1 единица Three.js = 1 дюйм**
- **Для работы в метрах**: используйте масштаб 0.0254
- **Для работы в сантиметрах**: используйте масштаб 2.54

---

## 📊 Сравнительная таблица

| Размер | Дюймы | Миллиметры | Сантиметры | Метры |
|--------|-------|------------|------------|-------|
| Ширина раскоса | 3.937 | 100 | 10 | 0.1 |
| Малая высота | 15.748 | 400 | 40 | 0.4 |
| Большая высота | 19.685 | 500 | 50 | 0.5 |

---

## 🔍 Как проверить самостоятельно

### В консоли браузера:

```javascript
import { createBraceR2 } from './assets/js/partGeometry_r2.js';

const mesh = createBraceR2();
const bbox = new THREE.Box3().setFromObject(mesh);
const size = new THREE.Vector3();
bbox.getSize(size);

console.log('Размеры в дюймах:', size);
console.log('Размеры в мм:', {
    x: size.x * 25.4,
    y: size.y * 25.4,
    z: size.z * 25.4
});
console.log('Размеры в см:', {
    x: size.x * 2.54,
    y: size.y * 2.54,
    z: size.z * 2.54
});
```

---

## ✅ Итог

**Единицы измерения:** **ДЮЙМЫ (inches)**

**Коэффициенты конвертации:**
- 1 дюйм = 25.4 мм
- 1 дюйм = 2.54 см
- 1 дюйм = 0.0254 м

**Типичные размеры раскосов:**
- Ширина: ~3.937" (10 см)
- Высота: ~15.748" - 19.685" (40-50 см)

---

**Дата анализа:** 2025-11-21  
**Источник:** raskos/r1.glb, r2.glb, r3.glb, r4.glb  
**Программа экспорта:** SimLab GLTF (SketchUp)  




