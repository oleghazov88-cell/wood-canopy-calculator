# 📊 АНАЛИЗ СИСТЕМЫ РАСКОСОВ - 23.11.2025

## 🎯 **ТЕКУЩЕЕ СОСТОЯНИЕ**

### **Статус:** ✅ Работает с простой геометрией и масштабированием

---

## 📐 **ГЕОМЕТРИЯ РАСКОСОВ**

### **Исходные размеры:**

```javascript
const beamDimensions = this.getBeamDimensions(postMaterial, postType);

// Для var-1, var-5:
beamDimensions = { width: 0.2, height: 0.2 }  // 200x200 мм

// Для var-2, var-3, var-4:
beamDimensions = getPostDimensions(postMaterial)  // Зависит от материала столбов
// Пример: "150x150" → { width: 0.15, height: 0.15 }  // 150x150 мм
```

### **Создание геометрии раскоса:**

```javascript
// Строки 2155-2159
const braceGeometry = new THREE.BoxGeometry(
    beamDimensions.width,           // Ширина: 0.15-0.2 м
    beamDimensions.height * 2,      // Длина: 0.3-0.4 м (удвоенное сечение)
    beamDimensions.height           // Глубина: 0.15-0.2 м
);
```

**Пример расчета (для столбов 150x150):**
- Ширина раскоса: 0.15 м (150 мм)
- Длина раскоса: 0.15 × 2 = 0.3 м (300 мм)
- Глубина раскоса: 0.15 м (150 мм)

---

## 🔢 **МАСШТАБИРОВАНИЕ**

### **Применяется масштаб 0.01 (1:100):**

```javascript
// Строки 2163, 2171
leftBrace.scale.set(0.01, 0.01, 0.01);
rightBrace.scale.set(0.01, 0.01, 0.01);
```

### **Финальные размеры после масштабирования:**

Для столбов 150x150:
- Ширина: 0.15 × 0.01 = **0.0015 м (1.5 мм)**
- Длина: 0.3 × 0.01 = **0.003 м (3 мм)**
- Глубина: 0.15 × 0.01 = **0.0015 м (1.5 мм)**

Для столбов 200x200:
- Ширина: 0.2 × 0.01 = **0.002 м (2 мм)**
- Длина: 0.4 × 0.01 = **0.004 м (4 мм)**
- Глубина: 0.2 × 0.01 = **0.002 м (2 мм)**

---

## 📍 **ПОЗИЦИОНИРОВАНИЕ**

### **Количество раскосов:**

```javascript
const postSpacing = this.currentPostSpacing;  // Обычно 2-3 метра
const postsAlongLength = Math.ceil(length / postSpacing) + 1;

// Для каждой позиции столба создаются 2 раскоса (левый и правый)
// Общее количество = postsAlongLength × 2
```

**Пример:** Навес 6 метров, шаг столбов 2 метра:
- Позиций столбов: Math.ceil(6 / 2) + 1 = 4
- Раскосов: 4 × 2 = **8 штук**

### **Координаты размещения:**

```javascript
// Левый раскос:
x: -width/2 - postDimensions.width/2  // Слева от оси навеса
y: beamHeight - 0.5                    // Под балкой на 0.5 м
z: -length/2 + (i × length / (postsAlongLength - 1))  // Вдоль навеса

// Правый раскос:
x: width/2 + postDimensions.width/2   // Справа от оси навеса
y: beamHeight - 0.5                   // Под балкой на 0.5 м
z: -length/2 + (i × length / (postsAlongLength - 1))  // Вдоль навеса
```

**Особенность:** Раскосы размещаются на половину сечения столба от оси столба.

---

## 🔄 **СИСТЕМА ЗАГРУЗКИ**

### **GLB модели отключены:**

```javascript
// Строки 189-191
async loadBraceGLB(braceType) {
    console.log(`Раскос ${braceType}: используем простые геометрические формы (GLB отключен)`);
    return null;  // ❌ Всегда возвращает null
}
```

### **Логика выбора:**

```javascript
// Строка 2120
const glbModel = await this.loadBraceGLB(braceType);

// Строка 2129
if (glbModel) {
    // ❌ Этот блок НИКОГДА не выполняется (glbModel всегда null)
} else {
    // ✅ Всегда используется простая геометрия
}
```

---

## 🎨 **МАТЕРИАЛЫ И ТЕНИ**

```javascript
// Материал раскосов
const woodMaterial = ...  // Деревянный материал из параметров

// Тени
leftBrace.castShadow = true;       // Отбрасывает тени
leftBrace.receiveShadow = true;    // Принимает тени
```

---

## 📊 **СРАВНЕНИЕ РАЗМЕРОВ**

| Элемент | Сечение | Длина | Масштаб | Финальный размер |
|---------|---------|-------|---------|------------------|
| **Столб** | 150x150 мм | 2-3 м | 1:1 | 150x150 мм |
| **Балка** | 150x150 мм | 4-6 м | 1:1 | 150x150 мм |
| **Ферма** | 50x50 мм | 3-4 м | 1:1 | 50x50 мм |
| **Раскос (до масштаба)** | 150x150 мм | 300 мм | - | 150x300 мм |
| **Раскос (после масштаба)** | 1.5x1.5 мм | 3 мм | 1:100 | **1.5x3 мм** ✅ |

---

## ⚠️ **ПРОБЛЕМЫ И РЕШЕНИЯ**

### **Проблема 1: Раскосы были огромными**
**Причина:** Использовалась высота навеса (`beamHeight/2`) вместо размеров балки  
**Решение:** Изменено на `beamDimensions.height * 2` (коммит `59af089`)

### **Проблема 2: Раскосы все еще слишком большие**
**Причина:** Размеры балки (150-200 мм) слишком большие для визуализации раскосов  
**Решение:** Применен масштаб 0.01 (1:100) (коммит `d3566bf`)

### **Проблема 3: GLB модели не загружались**
**Причина:** Проблемы с путями и размерами GLB файлов  
**Решение:** GLB полностью отключены, используется простая геометрия (коммит `171ad5e`)

---

## 🔍 **АНАЛИЗ КОДА**

### **Функция createBeamBraces()** (строки 2102-2181)

**Параметры:**
- `length` - длина навеса
- `width` - ширина навеса
- `height` - высота навеса
- `woodMaterial` - материал дерева
- `frontExtension` - вылет спереди
- `backExtension` - вылет сзади
- `postMaterial` - материал столбов (определяет размеры)
- `braceType` - тип раскоса (var-1, var-2, var-3, var-4)
- `postType` - тип столба

**Логика:**
1. Получить размеры балки (`beamDimensions`)
2. Рассчитать количество позиций (`postsAlongLength`)
3. Попытаться загрузить GLB модель (всегда возвращает null)
4. Создать простую геометрию раскоса
5. Применить масштаб 0.01
6. Разместить левый и правый раскосы для каждой позиции

---

## 💡 **РЕКОМЕНДАЦИИ**

### **Если раскосы слишком маленькие:**

Увеличить масштаб в строках 2163, 2171:
```javascript
leftBrace.scale.set(0.02, 0.02, 0.02);   // 1:50 вместо 1:100
rightBrace.scale.set(0.02, 0.02, 0.02);
```

### **Если раскосы слишком большие:**

Уменьшить масштаб:
```javascript
leftBrace.scale.set(0.005, 0.005, 0.005);  // 1:200 вместо 1:100
rightBrace.scale.set(0.005, 0.005, 0.005);
```

### **Для разных типов раскосов:**

Сейчас все типы (var-1, var-2, var-3, var-4) выглядят одинаково.  
Для визуального разнообразия можно:

1. **Изменить геометрию** в зависимости от `braceType`
2. **Изменить цвет** материала
3. **Изменить длину** раскоса

**Пример:**
```javascript
const lengthMultiplier = {
    'var-1': 2,   // Стандартный
    'var-2': 2.5, // Длиннее
    'var-3': 1.5, // Короче
    'var-4': 3    // Самый длинный
}[braceType];

const braceGeometry = new THREE.BoxGeometry(
    beamDimensions.width,
    beamDimensions.height * lengthMultiplier,
    beamDimensions.height
);
```

---

## 📝 **ИТОГИ**

### **✅ Что работает:**
- Простая геометрия раскосов
- Правильное позиционирование
- Корректные размеры с масштабированием
- Тени и материалы

### **⚠️ Что можно улучшить:**
- Визуальное разнообразие типов раскосов
- Более реалистичная геометрия (наклон, соединения)
- Оптимальный подбор масштаба

### **❌ Что не работает:**
- Загрузка GLB моделей (отключена намеренно)

---

## 📊 **СТАТИСТИКА**

- **Функция:** `createBeamBraces()` (строки 2102-2181)
- **Геометрия:** `THREE.BoxGeometry` (строки 2155-2159)
- **Масштаб:** 0.01 (1:100) (строки 2163, 2171)
- **Позиционирование:** Строки 2164, 2172
- **Количество кода:** ~80 строк
- **Зависимости:** `getBeamDimensions()`, `getPostDimensions()`, `loadBraceGLB()`

---

**Дата анализа:** 23 ноября 2025  
**Версия кода:** Коммит `d3566bf`  
**Статус:** ✅ Полный анализ завершен


