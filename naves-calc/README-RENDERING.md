# 🎨 Как рисуются элементы в калькуляторе навесов

## Краткое руководство по методам отрисовки

В калькуляторе используются **два разных метода** создания 3D элементов:

---

## 📦 Столбы - Программное рисование

### ✅ Рисуются вручную в Three.js

**БЕЗ ИМПОРТА** внешних файлов!

```javascript
// Создаем геометрию программно
const geometry = new THREE.BoxGeometry(0.15, 3.0, 0.15);
const material = new THREE.MeshStandardMaterial({ color: 0x8B4513 });
const post = new THREE.Mesh(geometry, material);
post.position.set(x, y, z);
scene.add(post);
```

**Преимущества:**
- ⚡ Мгновенное создание (<10ms)
- 💾 Минимум памяти (<100KB)
- 🚀 Высокая производительность
- ✅ Нет зависимостей от файлов

**Используется для:**
- Столбы
- Балки (продольные и поперечные)
- Кровля
- Простые геометрические элементы

---

## 📥 Раскосы - Импорт GLB

### ✅ Загружаются из файлов

**С ИМПОРТОМ** внешних GLB моделей!

```javascript
// Загружаем модель из файла
const loader = new THREE.GLTFLoader();
const gltf = await loader.load('../raskos/r2.glb');
const brace = gltf.scene;
brace.position.set(x, y, z);
scene.add(brace);
```

**Преимущества:**
- ✅ Детальные модели любой сложности
- ✅ Создание в 3D редакторе (Blender, SketchUp)
- ✅ Реалистичный внешний вид
- ✅ Возможность текстур и материалов

**Используется для:**
- Раскосы (4 типа)
- Декоративные элементы
- Сложные узлы

---

## 📊 Сравнение

| Характеристика | Столбы (программно) | Раскосы (импорт) |
|----------------|---------------------|------------------|
| Метод | THREE.BoxGeometry | GLTFLoader |
| Время создания | <10ms | 200-500ms |
| Память | <100KB | 2-5MB |
| Файлы | Не нужны | Нужны GLB |
| Детализация | Простая | Высокая |

---

## 🎯 Важно понимать

### Столбы = Программный код
```javascript
// ✅ Геометрия создается в коде
// ❌ НЕТ файлов
// ❌ НЕТ GLTFLoader
const postGeometry = new THREE.BoxGeometry(...);
```

### Раскосы = Внешние файлы
```javascript
// ✅ Модель загружается из файла
// ✅ ЕСТЬ GLTFLoader
// ✅ ЕСТЬ GLB файлы
await loader.load('r2.glb');
```

---

## 📚 Дополнительная информация

Подробнее читайте в файле [RENDERING-METHODS.md](RENDERING-METHODS.md)

---

**Дата:** 13.10.2025  
**Версия:** 2.0.1

