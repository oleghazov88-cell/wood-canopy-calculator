# 🧪 Руководство по тестированию MVP архитектуры

## Чек-лист проверки после рефакторинга

### ✅ 1. Проверка файловой структуры

Убедитесь, что все файлы созданы:

```
naves-calc/
├── assets/js/mvp/
│   ├── CanopyModel.js       ✓
│   ├── CanopyView.js        ✓
│   ├── Canopy3DRenderer.js  ✓
│   ├── CanopyPresenter.js   ✓
│   └── app.js               ✓
└── index.html               ✓ (обновлен)
```

**Команда для проверки:**
```powershell
cd "c:\Users\OLEG\Desktop\AZ\wood_canopy_calc\naves-calc\assets\js\mvp"
dir
```

---

### ✅ 2. Запуск в браузере

1. Откройте `index.html` в браузере
2. Откройте консоль разработчика (F12)
3. Проверьте логи инициализации:

**Ожидаемый вывод в консоли:**
```
=== Инициализация MVP архитектуры ===
✓ Model создан
✓ View создан
✓ 3D Renderer создан
✓ Presenter создан
Цены загружены: XX позиций
Инициализация калькулятора навесов...
3D сцена инициализирована
✓ Приложение инициализировано
=== Приложение готово к работе ===
Доступ через window.CanopyApp
```

---

### ✅ 3. Тестирование UI

#### Тест 1: Изменение слайдеров
- [ ] Перетащите слайдер "Длина навеса"
- [ ] Значение обновляется в реальном времени
- [ ] Цена пересчитывается автоматически
- [ ] 3D модель обновляется (с небольшой задержкой)

#### Тест 2: Выбор материалов
- [ ] Измените "Материал столбов"
- [ ] Цена изменяется
- [ ] Спецификация обновляется

#### Тест 3: Кнопки
- [ ] Нажмите "Сохранить" - должно появиться сообщение
- [ ] Нажмите "Заказать" - должно появиться сообщение

---

### ✅ 4. Тестирование API

Откройте консоль браузера и выполните:

```javascript
// 1. Проверка доступа к приложению
console.log(window.CanopyApp)
// Должен вывести объект с полями: presenter, model, view, renderer

// 2. Получение текущих данных
const data = window.CanopyApp.getCurrentData()
console.log(data)
// Должен вывести: { params, calculation, specification, prices }

// 3. Изменение параметра через Model
window.CanopyApp.model.updateParam('length', 150)
// Должно обновиться отображение

// 4. Расчет стоимости
const cost = window.CanopyApp.model.calculateCost()
console.log(cost)
// Должен вывести объект с полями: totalCost, materialsCost, etc.

// 5. Сброс к дефолтным значениям
window.CanopyApp.reset()
// Форма должна вернуться к начальным значениям

// 6. Сохранение
window.CanopyApp.save()
// Должно появиться сообщение "Расчет сохранен!"

// 7. Получение сохраненных
const saved = window.CanopyApp.getSaved()
console.log(saved)
// Должен вывести массив сохраненных расчетов
```

---

### ✅ 5. Тестирование 3D рендеринга

#### Проверка инициализации:
- [ ] Canvas отображается
- [ ] Видна 3D сцена с навесом
- [ ] Работает вращение мышью
- [ ] Работает зум колесиком мыши
- [ ] Земля и сетка отображаются

#### Проверка обновления модели:
- [ ] При изменении длины - навес становится длиннее
- [ ] При изменении ширины - навес становится шире
- [ ] При изменении высоты - столбы становятся выше
- [ ] При смене типа крыши - меняется форма крыши

---

### ✅ 6. Проверка расчетов

Установите значения:
- Длина: 10м
- Ширина: 6м
- Высота: 3м

**Проверьте в консоли:**
```javascript
const calc = window.CanopyApp.model.calculateCost()
console.log('Площадь:', calc.area) // Должно быть 60 м²
console.log('Количество столбов:', calc.postCount) // Зависит от шага столбов
console.log('Итого:', calc.totalCost) // Должна быть разумная цена
```

---

### ✅ 7. Тестирование производительности

#### В консоли:
```javascript
// Измерение времени инициализации
console.time('init')
// Перезагрузите страницу
// Время должно быть < 1 секунды

// Измерение времени расчета
console.time('calculate')
window.CanopyApp.model.calculateCost()
console.timeEnd('calculate')
// Время должно быть < 10ms

// Измерение времени обновления 3D
console.time('3d-update')
window.CanopyApp.renderer.update(window.CanopyApp.model.getParams())
console.timeEnd('3d-update')
// Время должно быть < 100ms
```

---

### ✅ 8. Тестирование ошибок

#### Проверка обработки ошибок:

```javascript
// 1. Несуществующий параметр
window.CanopyApp.model.updateParam('nonexistent', 100)
// Не должно крашнуться

// 2. Некорректное значение
window.CanopyApp.model.updateParam('length', -100)
// Должно обработаться корректно

// 3. Отсутствие цен
window.CanopyApp.model.prices = {}
window.CanopyApp.model.calculateCost()
// Должны использоваться дефолтные цены
```

---

### ✅ 9. Кроссбраузерное тестирование

Проверьте в:
- [ ] Chrome (latest)
- [ ] Firefox (latest)
- [ ] Edge (latest)
- [ ] Safari (если доступен)

**В каждом браузере:**
- [ ] Калькулятор загружается
- [ ] 3D модель отображается
- [ ] Все элементы управления работают

---

### ✅ 10. Мобильное тестирование

**В Chrome DevTools:**
1. Откройте DevTools (F12)
2. Включите режим устройства (Ctrl+Shift+M)
3. Выберите iPhone/iPad

**Проверьте:**
- [ ] Форма адаптируется под экран
- [ ] 3D canvas масштабируется корректно
- [ ] Слайдеры работают на тач-событиях
- [ ] Кнопки кликабельны

---

### ✅ 11. Сравнение со старой версией

**Откройте две вкладки:**

**Вкладка 1 (новая MVP):**
```html
<!-- Использует MVP модули -->
```

**Вкладка 2 (старая версия):**
```html
<!-- Переключите на backup версию -->
<script src="/naves-calc/assets/js/naves-calc.bundle.BACKUP.js"></script>
```

**Сравните:**
- [ ] Оба калькулятора дают одинаковые результаты
- [ ] UI выглядит идентично
- [ ] Функциональность не потеряна

---

## 🐛 Частые проблемы и решения

### Проблема 1: Консоль показывает "Cannot read property 'init'"

**Решение:**
```javascript
// Проверьте что все скрипты загружены
console.log(typeof CanopyModel) // должно быть 'function'
console.log(typeof CanopyView) // должно быть 'function'
console.log(typeof Canopy3DRenderer) // должно быть 'function'
console.log(typeof CanopyPresenter) // должно быть 'function'
```

### Проблема 2: 3D модель не отображается

**Решение:**
```javascript
// Проверьте загрузку Three.js
console.log(typeof THREE) // должно быть 'object'

// Проверьте canvas
console.log(document.querySelector('#nc-canvas')) // должен быть элемент
```

### Проблема 3: Цены не загружаются

**Решение:**
```javascript
// Проверьте путь к prices.json
// Должен отвечать по адресу: /naves-calc/upload/naves/prices.json

// Проверьте в консоли
window.CanopyApp.model.prices
// Если пуст, используются дефолтные цены
```

### Проблема 4: События не работают

**Решение:**
```javascript
// Проверьте привязку колбэков
console.log(window.CanopyApp.view.onParamChanged) // должна быть функция
console.log(window.CanopyApp.model.onDataChanged) // должна быть функция
```

---

## 📊 Метрики успеха

После тестирования проверьте:

- ✅ **Функциональность:** Все фичи работают как в старой версии
- ✅ **Производительность:** Время загрузки < 1с, расчеты < 10ms
- ✅ **Стабильность:** Нет ошибок в консоли
- ✅ **Совместимость:** Работает во всех основных браузерах
- ✅ **Удобство:** API понятен и прост в использовании

---

## 📝 Отчет о тестировании

После прохождения всех тестов заполните:

```
Дата: __________________
Тестировщик: __________________
Браузер: __________________
Версия: __________________

Результаты:
□ Все тесты пройдены
□ Найдены проблемы (описать ниже)

Проблемы:
_________________________________
_________________________________
_________________________________

Рекомендации:
_________________________________
_________________________________
_________________________________
```

---

## 🎯 Следующие шаги

После успешного тестирования:

1. **Если всё работает:**
   - Можно удалить backup файлы
   - Развернуть на production
   - Обновить документацию

2. **Если найдены проблемы:**
   - Зафиксировать баги
   - Создать issue
   - Исправить и повторить тестирование

3. **Для улучшения:**
   - Добавить unit тесты
   - Настроить автоматическое тестирование
   - Добавить E2E тесты (Cypress, Playwright)

---

**Удачи в тестировании! 🚀**

