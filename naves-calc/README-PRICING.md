# Система управления ценами (Pricing System)

Система позволяет обновлять цены в калькуляторе через Excel-файл без редактирования кода.

## 🚀 Быстрый старт

### 1. Подготовка сервера
Сервер необходим для конвертации Excel в JSON, который понимает калькулятор.

```bash
cd api
npm install
npm start
```
Сервер запустится на http://localhost:3000

### 2. Генерация шаблона
Если у вас нет Excel файла с ценами, создайте шаблон:

```bash
npm install xlsx
node scripts/generate-template.js
```
Файл будет создан в `upload/naves/prices.xlsx`.

### 3. Загрузка цен
1. Откройте `admin.html` в браузере.
2. Нажмите кнопку **"📊 Загрузить Excel"**.
3. Выберите ваш файл `prices.xlsx`.
4. После успешной загрузки цены обновятся мгновенно.

## 🛠 Архитектура

1. **Excel (Source)**: Менеджеры редактируют цены в Excel.
2. **API (Server)**: Принимает Excel, конвертирует в оптимизированный JSON.
3. **JSON (Cache)**: Сохраняется в `upload/naves/prices.json`.
4. **Frontend**: Калькулятор (`CanopyModel.js`) загружает `prices.json` при старте.

## 📋 Формат Excel

Обязательные колонки:
- **key**: Уникальный идентификатор (например, `post_glued_150x150`). НЕ МЕНЯТЬ!
- **price**: Цена (число).
- **name**: Название (для справки).
- **unit**: Единица измерения.

## ⚠️ Важно
Фронтенд использует `prices.json` напрямую. Если сервер API выключен, калькулятор продолжит работать, используя последний загруженный `prices.json`. Если файл недоступен, калькулятор использует внутренние цены по умолчанию.
