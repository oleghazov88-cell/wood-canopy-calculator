# 🏛️ Типы столбов - Визуальные варианты

## Дата создания: 13.10.2025
## Последнее обновление: 19.10.2025
## Версия: 2.1.0

---

## 📋 Описание

В калькуляторе доступны различные типы столбов с разной визуальной формой. Все столбы рисуются **программно в Three.js** без импорта внешних моделей.

---

## 🏗️ Доступные типы столбов

### Краткий обзор

| Тип | Название | Подпятник | Капитель | Статус |
|-----|----------|-----------|----------|--------|
| var-1 | Куст столбов с подпятником сверху | ✅ Сверху | ❌ | ✅ Реализован |
| var-2 | Простой столб | ❌ | ❌ | ✅ Реализован |
| var-3 | Столб с капителью | ❌ | ✅ | ✅ Реализован |
| var-4 | Полный декоративный | ✅ Снизу | ✅ | ✅ Реализован |
| var-5 | Столб с подпятником снизу | ✅ Снизу | ❌ | ✅ Реализован |
| var-6 | Резерв | - | - | ⏳ Планируется |

---

### **var-1** - Куст столбов с подпятником сверху ✅

**Описание:**
- Куст из 4 квадратных столбов 80×80 мм
- Расстояние между осями: 100 мм
- **Подпятник** (основание) сверху между столбом и балкой
- Подпятник шире куста на 40 мм с каждой стороны
- Высота подпятника: 40 мм
- **Уникальная конструкция** для особого визуального эффекта

---

### **var-5** - Столб с подпятником снизу ✅

**Описание:**
- Столб стандартного сечения
- **Подпятник** (основание) внизу
- Подпятник шире столба на 40 мм с каждой стороны
- Высота подпятника: 40 мм
- **var-5** - выбран по умолчанию

**Параметры подпятника:**
- Высота: **40 мм (0.04 м)**
- Ширина: сечение столба **+ 40 мм** с каждой стороны (всего +80 мм)
- Глубина: сечение столба **+ 40 мм** с каждой стороны (всего +80 мм)

**Пример размеров:**
- Столб 150×150 мм
- Подпятник: 230×40×230 мм (150 + 40×2 = 230 мм)

**Визуальная структура:**
```
        ┌──────┐
        │      │
        │      │  ← Столб (сечение зависит от материала)
        │      │
        ├──────┤  ← Нижняя часть столба входит в подпятник
      ┌─┴──────┴─┐
      │          │ ← Подпятник (шире на 40 мм с каждой стороны)
      └──────────┘
    ───────────────  ← Земля
```

**Код создания:**
```javascript
// Параметры
const hasFooting = postType === 'var-1' || postType === 'var-5';
const footingHeight = 0.04; // 40 мм
const footingOverhang = 0.04; // 40 мм с каждой стороны

// Размеры подпятника
const footingWidth = postDimensions.width + footingOverhang * 2;
const footingDepth = postDimensions.height + footingOverhang * 2;

// Создание подпятника
const footingGeometry = new THREE.BoxGeometry(
    footingWidth, 
    footingHeight, 
    footingDepth
);
const footing = new THREE.Mesh(footingGeometry, woodMaterial);
footing.position.set(x, footingHeight/2, z);

// Столб входит в подпятник (не поднимается на его высоту)
const postYPosition = height/2;
const post = new THREE.Mesh(postGeometry, woodMaterial);
post.position.set(x, postYPosition, z);
```

---

### **var-2** - Стандартный столб ✅

**Описание:**
- Простой столб без подпятника и без капители
- Сечение зависит от выбранного материала
- Самый простой базовый вариант

**Визуальная структура:**
```
        ┌──────┐
        │      │
        │      │  ← Столб
        │      │
        │      │
        └──────┘
    ───────────────  ← Земля
```

**Код создания:**
```javascript
// Простой столб без декоративных элементов
const postGeometry = new THREE.BoxGeometry(
    postDimensions.width, 
    height, 
    postDimensions.height
);
const post = new THREE.Mesh(postGeometry, woodMaterial);
post.position.set(x, height/2, z);
```

---

### **var-3** - Столб с капителью ✅

**Описание:**
- Столб с расширением вверху (капитель)
- Капитель для лучшей опоры балки и эстетики
- Капитель шире столба на 50 мм с каждой стороны
- Высота капители: 80 мм

**Параметры капители:**
- Высота: **80 мм (0.08 м)**
- Ширина: сечение столба **+ 50 мм** с каждой стороны (всего +100 мм)
- Глубина: сечение столба **+ 50 мм** с каждой стороны (всего +100 мм)

**Пример размеров:**
- Столб 150×150 мм
- Капитель: 250×80×250 мм (150 + 50×2 = 250 мм)

**Визуальная структура:**
```
      ┌──────────┐
      │          │ ← Капитель (расширение 80 мм)
      └──┬────┬──┘
        │      │
        │      │  ← Столб
        │      │
        │      │
        └──────┘
    ───────────────  ← Земля
```

**Код создания:**
```javascript
// Параметры
const capitalHeight = 0.08; // 80 мм
const capitalOverhang = 0.05; // 50 мм с каждой стороны

// Размеры капители
const capitalWidth = postDimensions.width + capitalOverhang * 2;
const capitalDepth = postDimensions.height + capitalOverhang * 2;

// Создание капители
const capitalGeometry = new THREE.BoxGeometry(
    capitalWidth, 
    capitalHeight, 
    capitalDepth
);
const capital = new THREE.Mesh(capitalGeometry, woodMaterial);
// Капитель размещается на верху столба
capital.position.set(x, height - capitalHeight/2, z);

// Столб стандартный
const post = new THREE.Mesh(postGeometry, woodMaterial);
post.position.set(x, height/2, z);
```

---

### **var-4** - Столб с подпятником и капителью ✅

**Описание:**
- Комбинация подпятника внизу и капители вверху
- Полный декоративный столб
- Подпятник: шире на 40 мм, высота 40 мм
- Капитель: шире на 50 мм, высота 80 мм

**Параметры:**
- **Подпятник:**
  - Высота: 40 мм (0.04 м)
  - Расширение: +40 мм с каждой стороны
- **Капитель:**
  - Высота: 80 мм (0.08 м)
  - Расширение: +50 мм с каждой стороны

**Пример размеров:**
- Столб 150×150 мм
- Подпятник: 230×40×230 мм
- Капитель: 250×80×250 мм

**Визуальная структура:**
```
      ┌──────────┐
      │          │ ← Капитель (80 мм)
      └──┬────┬──┘
        │      │
        │      │  ← Столб
        │      │
        ├──────┤  ← Нижняя часть входит в подпятник
      ┌─┴──────┴─┐
      │          │ ← Подпятник (40 мм)
      └──────────┘
    ───────────────  ← Земля
```

**Код создания:**
```javascript
// Параметры подпятника
const footingHeight = 0.04;
const footingOverhang = 0.04;
const footingWidth = postDimensions.width + footingOverhang * 2;
const footingDepth = postDimensions.height + footingOverhang * 2;

// Параметры капители
const capitalHeight = 0.08;
const capitalOverhang = 0.05;
const capitalWidth = postDimensions.width + capitalOverhang * 2;
const capitalDepth = postDimensions.height + capitalOverhang * 2;

// Создание подпятника
const footing = new THREE.Mesh(footingGeometry, woodMaterial);
footing.position.set(x, footingHeight/2, z);

// Столб
const post = new THREE.Mesh(postGeometry, woodMaterial);
post.position.set(x, height/2, z);

// Капитель
const capital = new THREE.Mesh(capitalGeometry, woodMaterial);
capital.position.set(x, height - capitalHeight/2, z);
```

---

## 🔧 Технические детали

### Общие правила

1. **Все типы рисуются программно** (THREE.BoxGeometry)
2. **Сечение столба** зависит от выбранного материала
3. **Высота столба** задается пользователем
4. **Все элементы** учитывают материал и цвет

### Размеры сечений по материалам

```javascript
getPostDimensions(postMaterial) {
    switch(postMaterial) {
        case 'glued-100x100': 
            return { width: 0.1, height: 0.1 };      // 100×100 мм
        case 'glued-150x150': 
            return { width: 0.15, height: 0.15 };    // 150×150 мм
        case 'glued-200x200': 
            return { width: 0.2, height: 0.2 };      // 200×200 мм
        case 'glued-240x140': 
            return { width: 0.24, height: 0.14 };    // 240×140 мм
        case 'planed-90x90': 
            return { width: 0.09, height: 0.09 };    // 90×90 мм
        case 'planed-140x140': 
            return { width: 0.14, height: 0.14 };    // 140×140 мм
        case 'planed-190x190': 
            return { width: 0.19, height: 0.19 };    // 190×190 мм
    }
}
```

### Влияние на другие элементы

Когда столб имеет подпятник (var-1 или var-5), подпятник НЕ влияет на высоту конструкции:

**Важно:** Столб входит в подпятник, а не стоит на нём. Поэтому высота конструкции остается неизменной.

**1. Балки:**
```javascript
// Подпятник не влияет на высоту балок
const beamHeight = height;
```

**2. Раскосы:**
```javascript
// Подпятник не влияет на высоту раскосов
const braceHeight = height;
```

**3. Фермы:**
```javascript
// Подпятник не влияет на высоту ферм
const trussHeight = height + beamDimensions.height / 2;
```

**4. Кровля:**
```javascript
// Подпятник не влияет на высоту кровли
const he = height + beamDimensions.height / 2;
```

---

## 📐 Геометрия подпятника (var-1 и var-5)

### Расчет размеров

```javascript
// Исходное сечение столба
const postWidth = 0.15;   // 150 мм
const postDepth = 0.15;   // 150 мм

// Параметры подпятника
const footingOverhang = 0.04;  // 40 мм с каждой стороны
const footingHeight = 0.04;     // 40 мм высота

// Размеры подпятника
const footingWidth = postWidth + footingOverhang * 2;   // 0.15 + 0.08 = 0.23 м (230 мм)
const footingDepth = postDepth + footingOverhang * 2;   // 0.15 + 0.08 = 0.23 м (230 мм)
```

### Позиционирование

```javascript
// Подпятник на земле
footing.position.y = footingHeight / 2;  // 0.02 м (центр подпятника)

// Столб входит в подпятник (не поднимается)
post.position.y = height / 2;  // Стандартное позиционирование
```

---

## 🎨 Примеры для разных материалов (var-1 и var-5)

### Клееный брус 100×100 мм + подпятник

```
Столб:      100 × высота × 100 мм
Подпятник:  180 × 40 × 180 мм
```

### Клееный брус 150×150 мм + подпятник

```
Столб:      150 × высота × 150 мм
Подпятник:  230 × 40 × 230 мм
```

### Клееный брус 200×200 мм + подпятник

```
Столб:      200 × высота × 200 мм
Подпятник:  280 × 40 × 280 мм
```

### Строганый брус 90×90 мм + подпятник

```
Столб:      90 × высота × 90 мм
Подпятник:  170 × 40 × 170 мм
```

---

## ✅ Проверочный список

### Реализованные типы столбов:

- [x] **var-1** - Куст столбов с подпятником сверху
- [x] **var-2** - Простой столб без декора
- [x] **var-3** - Столб с капителью
- [x] **var-4** - Полный декоративный (подпятник + капитель)
- [x] **var-5** - Столб с подпятником снизу (по умолчанию)
- [ ] **var-6** - Резерв для будущих вариантов

### При добавлении нового типа столба убедитесь:

- [x] Создана геометрия столба (BoxGeometry)
- [x] Определены размеры специальных элементов (подпятник, капитель)
- [x] Правильное позиционирование по оси Y
- [x] Обновлены все зависимые функции:
  - [x] createPosts - основная функция создания
  - [x] createLongitudinalBeams - балки
  - [x] createBeamBraces - раскосы
  - [x] createTrusses - фермы
  - [x] createRoofCovering - кровля
- [x] Обновлен UI (index.html)
- [x] Добавлена документация
- [ ] Протестированы все материалы столбов с новыми типами

---

## 📚 Связанные документы

- [RENDERING-METHODS.md](RENDERING-METHODS.md) - Методы отрисовки
- [POST_IMPORT.md](POST_IMPORT.md) - Старая документация (устарела)
- [POST_SCALING.md](POST_SCALING.md) - Масштабирование столбов

---

## 🎉 Итоговая сводка

### Реализовано в версии 2.1.0:

✅ **var-1** - Куст столбов с подпятником сверху (работает)  
✅ **var-2** - Простой столб без декора (НОВЫЙ!)  
✅ **var-3** - Столб с капителью (НОВЫЙ!)  
✅ **var-4** - Полный декоративный столб (НОВЫЙ!)  
✅ **var-5** - Столб с подпятником снизу (работает)  

### Технические характеристики:

- **Подпятник:** 40 мм высота, +40 мм расширение с каждой стороны
- **Капитель:** 80 мм высота, +50 мм расширение с каждой стороны
- **Все типы:** Программная отрисовка в Three.js (без импорта моделей)
- **Материалы:** Поддержка всех сечений (100×100 до 240×140)

### Важные особенности:

✅ Столб входит в подпятник, не стоит на нём  
✅ Капитель размещается на верху столба  
✅ Подпятник НЕ влияет на общую высоту конструкции  
✅ Все элементы адаптируются под выбранный материал столба  

---

**Дата создания:** 13.10.2025  
**Последнее обновление:** 19.10.2025  
**Версия:** 2.1.0  
**Автор:** Wood Canopy Calc Team

🎊 **Все 5 типов столбов реализованы и готовы к использованию!**

