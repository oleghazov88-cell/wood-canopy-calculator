# 🔌 Backend API для калькулятора навесов

Готовые решения для обработки заказов с сайта.

---

## 📁 Структура

```
api/
├── orders.php          # PHP endpoint
├── config.example.php  # Пример конфигурации PHP
├── server.js           # Node.js сервер
├── config.json         # Конфигурация Node.js
├── package.json        # Зависимости Node.js
└── orders/             # Папка для сохранения заказов (создается автоматически)
```

---

## 🚀 Вариант 1: PHP (рекомендуется для простоты)

### Требования:
- PHP 7.4+
- Веб-сервер (Apache/Nginx)

### Установка:

1. **Скопируйте файлы на сервер:**
   ```bash
   # Загрузите папку api/ на ваш сервер
   # Например в: /var/www/html/naves-calc/api/
   ```

2. **Настройте конфигурацию:**
   ```bash
   cp config.example.php config.php
   nano config.php
   ```
   
   Измените в `config.php`:
   ```php
   define('ADMIN_EMAIL', 'your-email@example.com');
   ```

3. **Настройте права доступа:**
   ```bash
   chmod 755 api/
   chmod 755 api/orders.php
   chmod 777 api/orders/  # Папка должна быть доступна для записи
   ```

4. **Обновите путь в калькуляторе:**
   
   В файле `naves-calc/index.html` (строка 1865):
   ```javascript
   const orderManager = new OrderManager('/naves-calc/api/orders.php');
   ```

5. **Проверьте работу:**
   ```bash
   curl -X POST http://your-site.com/naves-calc/api/orders.php \
     -H "Content-Type: application/json" \
     -d '{"customerData":{"name":"Test","phone":"123"},"calculationData":{}}'
   ```

### Опциональные настройки:

#### Email уведомления:
В `config.php` раскомментируйте и настройте:
```php
define('ADMIN_EMAIL', 'manager@example.com');
```

В `orders.php` раскомментируйте строку:
```php
require_once __DIR__ . '/config.php';
```

#### Telegram уведомления:

1. Создайте бота через [@BotFather](https://t.me/botfather)
2. Получите токен бота
3. Получите ваш Chat ID через [@userinfobot](https://t.me/userinfobot)
4. Добавьте в `config.php`:
   ```php
   define('TELEGRAM_BOT_TOKEN', 'YOUR_BOT_TOKEN');
   define('TELEGRAM_CHAT_ID', 'YOUR_CHAT_ID');
   ```

---

## 🚀 Вариант 2: Node.js (для больших нагрузок)

### Требования:
- Node.js 14+
- npm или yarn

### Установка:

1. **Перейдите в папку api:**
   ```bash
   cd naves-calc/api/
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Настройте конфигурацию:**
   ```bash
   nano config.json
   ```
   
   Измените порт и настройки email/telegram:
   ```json
   {
     "port": 3000,
     "email": {
       "enabled": true,
       "to": "manager@example.com",
       ...
     }
   }
   ```

4. **Запустите сервер:**
   ```bash
   # Режим разработки (автоперезагрузка)
   npm run dev
   
   # Продакшн режим
   npm start
   ```

5. **Настройте как systemd сервис (Linux):**
   ```bash
   sudo nano /etc/systemd/system/naves-api.service
   ```
   
   Содержимое:
   ```ini
   [Unit]
   Description=Naves Calculator API
   After=network.target
   
   [Service]
   Type=simple
   User=www-data
   WorkingDirectory=/var/www/html/naves-calc/api
   ExecStart=/usr/bin/node server.js
   Restart=on-failure
   
   [Install]
   WantedBy=multi-user.target
   ```
   
   Запустите:
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl enable naves-api
   sudo systemctl start naves-api
   sudo systemctl status naves-api
   ```

6. **Настройте Nginx proxy (опционально):**
   ```nginx
   location /api/ {
       proxy_pass http://localhost:3000/api/;
       proxy_http_version 1.1;
       proxy_set_header Upgrade $http_upgrade;
       proxy_set_header Connection 'upgrade';
       proxy_set_header Host $host;
       proxy_cache_bypass $http_upgrade;
   }
   ```

7. **Обновите путь в калькуляторе:**
   
   В файле `naves-calc/index.html` (строка 1865):
   ```javascript
   const orderManager = new OrderManager('http://your-site.com:3000/api/orders');
   // или через proxy:
   const orderManager = new OrderManager('/api/orders');
   ```

---

## 📊 Что делает API

### PHP (orders.php):
1. ✅ Принимает POST запрос с данными заказа
2. ✅ Валидирует обязательные поля (имя, телефон)
3. ✅ Генерирует уникальный ID заказа
4. ✅ Сохраняет заказ в JSON файл
5. ✅ Добавляет запись в лог (`orders.log`)
6. ✅ Отправляет email уведомление (опционально)
7. ✅ Отправляет Telegram уведомление (опционально)
8. ✅ Возвращает успешный ответ с ID заказа

### Node.js (server.js):
То же самое + дополнительно:
- ✅ `GET /api/orders` - получить список всех заказов
- ✅ `GET /api/orders/:id` - получить конкретный заказ
- ✅ `GET /health` - проверка работоспособности

---

## 📁 Структура заказа

Заказы сохраняются в папке `orders/` в формате:
```
ORD-20251105-1699213185-1234.json
```

Содержимое файла:
```json
{
  "orderId": "ORD-20251105-1699213185-1234",
  "timestamp": 1699213185000,
  "date": "2025-11-05 22:00:00",
  "ip": "192.168.1.1",
  "userAgent": "Mozilla/5.0...",
  "customerData": {
    "name": "Иван Иванов",
    "phone": "+7 (999) 123-45-67",
    "email": "ivan@example.com",
    "address": "Москва, ул. Примерная, 10",
    "comment": "Доставка в выходные"
  },
  "calculationData": {
    "length": 11,
    "width": 6,
    "height": 3,
    "area": "66.00",
    "roofType": "var-2",
    "postMaterial": "glued-150x150",
    "roofingMaterial": "metal-grandline",
    "totalPrice": "150 000 ₽"
  }
}
```

---

## 🧪 Тестирование

### Проверка PHP endpoint:
```bash
curl -X POST http://localhost/naves-calc/api/orders.php \
  -H "Content-Type: application/json" \
  -d '{
    "customerData": {
      "name": "Test User",
      "phone": "+7 999 123-45-67",
      "email": "test@example.com"
    },
    "calculationData": {
      "length": 11,
      "width": 6,
      "totalPrice": "150000 ₽"
    }
  }'
```

### Проверка Node.js endpoint:
```bash
# Проверка работы сервера
curl http://localhost:3000/health

# Отправка заказа
curl -X POST http://localhost:3000/api/orders \
  -H "Content-Type: application/json" \
  -d '{
    "customerData": {
      "name": "Test User",
      "phone": "+7 999 123-45-67"
    },
    "calculationData": {
      "totalPrice": "150000 ₽"
    }
  }'

# Получение списка заказов
curl http://localhost:3000/api/orders
```

---

## 🔒 Безопасность

### Рекомендации:

1. **Ограничьте доступ к папке orders:**
   ```apache
   # .htaccess в папке orders/
   Deny from all
   ```

2. **Используйте HTTPS:**
   - Настройте SSL сертификат (Let's Encrypt)
   - Принимайте заказы только через HTTPS

3. **Добавьте rate limiting:**
   - PHP: используйте session или Redis
   - Node.js: установите `express-rate-limit`

4. **Валидация данных:**
   - Проверяйте формат телефона
   - Санитизируйте входные данные
   - Ограничивайте размер запроса

5. **Мониторинг:**
   - Отслеживайте лог файл `orders.log`
   - Настройте алерты на ошибки

---

## 📧 Настройка Email

### Gmail:
1. Включите 2FA в аккаунте Google
2. Создайте App Password: https://myaccount.google.com/apppasswords
3. Используйте App Password в конфигурации

### Yandex:
```json
{
  "smtp": {
    "host": "smtp.yandex.ru",
    "port": 587,
    "secure": false,
    "user": "your-email@yandex.ru",
    "pass": "your-password"
  }
}
```

### Mail.ru:
```json
{
  "smtp": {
    "host": "smtp.mail.ru",
    "port": 465,
    "secure": true,
    "user": "your-email@mail.ru",
    "pass": "your-password"
  }
}
```

---

## 📱 Настройка Telegram

1. **Создайте бота:**
   - Откройте [@BotFather](https://t.me/botfather)
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Сохраните токен

2. **Получите Chat ID:**
   - Откройте [@userinfobot](https://t.me/userinfobot)
   - Отправьте любое сообщение
   - Скопируйте ваш ID

3. **Добавьте бота в группу (опционально):**
   - Создайте группу
   - Добавьте бота в группу
   - Отправьте сообщение в группу
   - Получите Chat ID группы через API

---

## 🐛 Решение проблем

### PHP:

**Ошибка: "Permission denied"**
```bash
sudo chmod 777 api/orders/
```

**Ошибка: "Cannot write to file"**
- Проверьте права доступа
- Проверьте владельца файлов (`chown www-data:www-data`)

**Email не отправляются:**
- Проверьте настройки SMTP
- Проверьте логи PHP (`/var/log/php-errors.log`)
- Убедитесь что `mail()` функция работает

### Node.js:

**Ошибка: "EADDRINUSE"**
- Порт уже занят
- Измените порт в `config.json`
- Или остановите процесс: `lsof -i :3000` → `kill -9 PID`

**Модули не установлены:**
```bash
npm install
```

**Ошибки при отправке email:**
- Проверьте настройки SMTP в `config.json`
- Проверьте App Password для Gmail

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сервера
2. Проверьте консоль браузера (F12)
3. Убедитесь что endpoint доступен
4. Проверьте права доступа к файлам

---

## ✅ Готово!

После настройки backend ваш калькулятор будет полностью функционален:
- ✅ Сохранение расчетов (localStorage)
- ✅ Оформление заказов (backend)
- ✅ Экспорт в PDF
- ✅ Уведомления менеджерам

**Следующий шаг:** Протестируйте отправку заказа с сайта!

