# 🏗️ ТРЕУГОЛЬНАЯ ФЕРМА С УСОВЫМИ ПОДРЕЗКАМИ

**Дата:** 27 октября 2025  
**Версия:** 2.2.0  
**Статус:** ✅ Реализовано

---

## 🎯 ОПИСАНИЕ

Реализована **профессиональная треугольная ферма** с усовыми подрезками (miter cuts) на основе строительных стандартов.

### Ключевые особенности:

1. ✅ **Усовые подрезки** на поясах в узлах
2. ✅ **Раскосы с Т-стыками** (без усов)
3. ✅ **Сегментированная конструкция** (N=8 сегментов)
4. ✅ **Автоматический расчет** углов и смещений
5. ✅ **Симметричная структура**

---

## 📐 МАТЕМАТИЧЕСКАЯ МОДЕЛЬ

### Утилиты:

#### 1. Нормализация вектора
```javascript
unit(v) {
    return v.clone().normalize();
}
```

#### 2. Угол между векторами
```javascript
angle(u, v) {
    return Math.acos(THREE.MathUtils.clamp(u.dot(v), -1, 1));
}
```

#### 3. Смещение для усовой подрезки
```javascript
miterOffset(widthInPlane, phi) {
    return (widthInPlane / 2) / Math.tan(phi / 2);
}
```

**Формула:**
```
s = (b / 2) / tan(φ / 2)
```

где:
- `b` - ширина сечения в плоскости подрезки
- `φ` - угол между осями сходящихся стержней
- `s` - смещение для укорачивания стержня

---

## 🔧 СТРУКТУРА ФЕРМЫ

### Узлы:

Ферма разделена на **N сегментов** (по умолчанию N=8):

```javascript
// Нижний пояс
bot[i] = new THREE.Vector3(x, bottomChordHeight, 0)
где x = -L/2 + L * i / N

// Верхний пояс (треугольная форма)
top[i] = new THREE.Vector3(x, yTop, 0)
где yTop = bottomChordHeight + H * (1 - |2*i/N - 1|)
```

### Визуализация узлов (N=8):

```
                     4 (конек, y=max)
                    /│\
                   / │ \
                  /  │  \
                 /   │   \
                /    │    \
              3      │      5
             /       │       \
            /        │        \
           /         │         \
          /          │          \
         /           │           \
       2             │             6
      /              │              \
     /               │               \
    /                │                \
   /                 │                 \
  1                  │                  7
 /                   │                   \
/____________________│____________________\
0                    4                    8
      ◄────── Нижний пояс (bot) ──────►
```

---

## 🏗️ ЭЛЕМЕНТЫ ФЕРМЫ

### 1. ВЕРХНИЙ ПОЯС (стропильные ноги)

Сегменты: `top[i]` → `top[i+1]` (всего N сегментов)

**Особенности:**
- С усовыми подрезками в узлах
- Автоматический расчет смещений `s0` и `s1`
- Симметричная подрезка слева и справа от конька

### 2. НИЖНИЙ ПОЯС

Сегменты: `bot[i]` → `bot[i+1]` (всего N сегментов)

**Особенности:**
- С усовыми подрезками в узлах
- Горизонтальная линия
- Равномерное деление на сегменты

### 3. РАСКОСЫ (решетка)

Чередующиеся диагонали с **Т-стыком**:

```javascript
if (i % 2 === 0) {
    // Четные: снизу i → вверх i+1
    bot[i] → top[i+1]
} else {
    // Нечетные: сверху i → вниз i+1
    top[i] → bot[i+1]
}
```

**Особенности:**
- БЕЗ усовых подрезок (Т-стык)
- Переменная длина
- Образуют решетчатую структуру

---

## 🎨 ВИЗУАЛИЗАЦИЯ ФЕРМЫ

```
                        КОНЕК
                          ▲
                     ┌───/│\───┐
                    /   / │ \   \
      Верхний пояс /   /  │  \   \  (с усовыми подрезками)
    (сегменты)    /   /   │   \   \
                 /   /    │    \   \
           ┌───/   /     │     \   \───┐
          /   /   /      │      \   \   \
         /   /   /       │       \   \   \
        /   /   /        │        \   \   \
       /   / ╱ /         │         \ ╲ \   \
      /   / ╱ /          │          \ ╲ \   \
     /   / ╱ /           │           \ ╲ \   \
    /   / ╱ /            │            \ ╲ \   \
   /___/_╱_/____Раскосы__│__Раскосы____\_╲_\___\
  ◄────────── Нижний пояс (с усами) ──────────────►
  0    1    2    3    4    5    6    7    8
```

**Раскосы:** чередующиеся диагонали (/\\/\\/\\)

---

## 🔍 УСОВЫЕ ПОДРЕЗКИ

### Принцип работы:

Когда два стержня сходятся в узле под углом `φ`, каждый стержень укорачивается на расстояние `s`:

```
         Узел
           │
    ───────┼───────  ← без усовой подрезки (щель!)
    ───────╪───────  
           │

    ─────╱   ╲─────  ← с усовой подрезкой (идеальный стык!)
         ╲   ╱
          ╲ ╱
           X узел
```

### Алгоритм:

1. Для каждого внутреннего узла цепочки (пояса):
   - Найти два сходящихся сегмента
   - Вычислить угол `φ` между их осями
   - Рассчитать смещение `s = (b/2) / tan(φ/2)`
   - Укоротить левый сегмент справа на `s`
   - Укоротить правый сегмент слева на `s`

2. Применяется только к поясам (type='top', type='bot')

3. Раскосы остаются без подрезок (Т-стык)

---

## 📊 ФУНКЦИЯ `makeMember()`

Создает брус с подрезками на концах:

```javascript
makeMember(P, Q, b, t, sStart = 0, sEnd = 0, material)
```

**Параметры:**
- `P` - начальная точка (Vector3)
- `Q` - конечная точка (Vector3)
- `b` - высота сечения
- `t` - ширина сечения
- `sStart` - смещение подрезки на начале
- `sEnd` - смещение подрезки на конце
- `material` - материал Three.js

**Алгоритм:**
1. Вычислить направление `d = unit(Q - P)`
2. Вычислить эффективную длину `L = |Q-P| - sStart - sEnd`
3. Создать геометрию `BoxGeometry(L, b, t)` вдоль оси X
4. Вычислить центр с учетом подрезок
5. Повернуть брус вдоль направления `d`

---

## 🧮 ФУНКЦИЯ `applyMiterOnChain()`

Применяет усовые подрезки ко всей цепочке узлов:

```javascript
const applyMiterOnChain = (points, type) => {
    const segs = members.filter(m => m.type === type);
    const sAtEnd = new Array(segs.length).fill(null).map(() => ({ s0: 0, s1: 0 }));
    
    for (let k = 0; k < segs.length - 1; k++) {
        const a = segs[k];
        const bSeg = segs[k + 1];
        const u_in = this.unit(a.P.clone().sub(a.Q));
        const u_out = this.unit(bSeg.Q.clone().sub(bSeg.P));
        
        const phi = this.angle(u_in, u_out);
        if (phi < 1e-3 || Math.PI - phi < 1e-3) continue;
        
        const s = this.miterOffset(b, phi);
        sAtEnd[k].s1 = Math.max(sAtEnd[k].s1, s);
        sAtEnd[k + 1].s0 = Math.max(sAtEnd[k + 1].s0, s);
    }
    
    for (let k = 0; k < segs.length; k++) {
        segs[k].s0 = sAtEnd[k].s0;
        segs[k].s1 = sAtEnd[k].s1;
    }
};
```

---

## 📈 ПАРАМЕТРЫ ФЕРМЫ

### Входные параметры:

```javascript
{
    span: L,              // Пролет фермы (ширина навеса)
    rise: H,              // Подъем фермы (высота конька)
    sectionSize: {        // Размеры сечения
        height: b,        // Высота сечения (0.12 м)
        width: t          // Ширина сечения (0.06 м)
    },
    bottomChordHeight: y  // Высота нижнего пояса
}
```

### Константы:

- `N = 8` - количество сегментов (четное для симметрии)

### Вычисляемые значения:

```javascript
// Координата X узла i
x[i] = -L/2 + L * i / N

// Высота верхнего узла i (треугольная форма)
yTop[i] = bottomChordHeight + H * (1 - |2*i/N - 1|)

где |2*i/N - 1| дает:
  i=0:   1.0  (края)
  i=N/2: 0.0  (конек)
  i=N:   1.0  (края)
```

---

## ✅ ПРЕИМУЩЕСТВА НОВОЙ КОНСТРУКЦИИ

### 1. Реалистичность
- ✅ Соответствует профессиональным строительным стандартам
- ✅ Усовые подрезки на поясах (как в реальных фермах)
- ✅ Раскосы с Т-стыком

### 2. Точность
- ✅ Автоматический расчет всех углов и смещений
- ✅ Идеальная стыковка без щелей
- ✅ Учет геометрии сечения

### 3. Гибкость
- ✅ Легко изменить количество сегментов (N)
- ✅ Работает для любых пролетов и высот
- ✅ Универсальные размеры сечения

### 4. Производительность
- ✅ Эффективная геометрия (BoxGeometry)
- ✅ Минимум вычислений
- ✅ Кэширование материалов

---

## 🔬 СРАВНЕНИЕ ВЕРСИЙ

### v2.1.2 (Предыдущая):
- Простая треугольная ферма
- 3 элемента (нижний пояс + 2 стропилы)
- Торцевые срезы на концах
- Без раскосов

### v2.2.0 (Текущая):
- Профессиональная сегментированная ферма
- N*2 + N элементов (верхний пояс + нижний пояс + раскосы)
- Усовые подрезки в каждом узле
- Решетчатая структура с раскосами
- Автоматический расчет

---

## 📝 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Элементы одной фермы (N=8):

| Тип | Количество | Подрезки |
|-----|-----------|----------|
| Верхний пояс | 8 сегментов | ✅ Усовые |
| Нижний пояс | 8 сегментов | ✅ Усовые |
| Раскосы | 8 элементов | ❌ Т-стык |
| **ВСЕГО** | **24 элемента** | |

### Узлы:
- Нижний пояс: 9 узлов (0..8)
- Верхний пояс: 9 узлов (0..8)
- **ВСЕГО:** 18 узлов

### Геометрия одного бруса:
- Примитив: BoxGeometry
- Вершины: 8
- Треугольники: 12
- Материал: woodMaterial

---

## 🧪 ПРОВЕРКА

### Что проверить после обновления:

1. ✅ Ферма имеет треугольную форму
2. ✅ Видны усовые подрезки на поясах
3. ✅ Раскосы чередуются (решетка)
4. ✅ Симметрия относительно центра
5. ✅ Нет щелей в узлах поясов
6. ✅ Количество элементов = 24

---

## 🎯 ФОРМУЛЫ ДЛЯ ПРОВЕРКИ

### Длина верхнего пояса (без подрезок):
```
L_top = 2 * √((L/2)² + H²)
```

### Угол наклона стропил:
```
α = arctan(H / (L/2))
```

### Общая длина всех стержней:
```
L_total ≈ L + 2*√((L/2)² + H²) + N*L_раскос
```

---

## 🔧 НАСТРОЙКА

### Изменить количество сегментов:

В функции `buildTruss()` измените:
```javascript
const N = 8; // ← здесь
```

**Рекомендации:**
- N должно быть четным (для симметрии)
- N ≥ 4 (минимум для раскосов)
- N ≤ 16 (для производительности)

---

## 📚 ССЫЛКИ НА КОД

### Файл: `naves-calc/assets/js/naves-calc.bundle.js`

**Функции:**
- `unit()` - строка 1995
- `angle()` - строка 2000
- `miterOffset()` - строка 2005
- `makeMember()` - строка 2010
- `buildTruss()` - строка 2041

**Утилиты:**
- строки 1992-2038

**Построение фермы:**
- строки 2041-2130

---

## 💡 ДАЛЬНЕЙШИЕ УЛУЧШЕНИЯ

### Приоритет 1:
- [ ] Добавить параметр N в интерфейс
- [ ] Визуализация узлов
- [ ] Различные материалы для поясов и раскосов

### Приоритет 2:
- [ ] Другие типы решеток (крест-накрест, полурешетка)
- [ ] Опция без раскосов (только пояса)
- [ ] Расчет нагрузок

### Приоритет 3:
- [ ] Экспорт геометрии фермы
- [ ] Спецификация стержней
- [ ] Чертеж узлов

---

**Конец документа**

Реализовано: 27 октября 2025  
Версия: 2.2.0  
Статус: ✅ Готово к использованию
