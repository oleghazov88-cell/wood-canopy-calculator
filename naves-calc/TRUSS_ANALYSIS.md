# 🏗️ ДЕТАЛЬНЫЙ АНАЛИЗ СИСТЕМЫ ФЕРМ
**Дата анализа:** 25 октября 2025  
**Версия:** 2.1.0  
**Статус:** Реализован двускатный тип с торцевым соединением

---

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ

### Реализованные типы ферм:
- ✅ **var-2: Двускатный** (Triangle/Gable Truss)
- ⏸️ **var-1: Односкатный** (Shed/Mono-pitch) - НЕ РЕАЛИЗОВАН
- ⏸️ **var-3: Арочный** (Arched) - НЕ РЕАЛИЗОВАН

---

## 🔧 СТРУКТУРА ДВУСКАТНОЙ ФЕРМЫ (var-2)

### 1. Узлы фермы
```javascript
const nodes = {
    leftBottom:    { x: -L/2, y: bottomChordHeight },      // Левый опорный узел
    rightBottom:   { x:  L/2, y: bottomChordHeight },      // Правый опорный узел
    ridge:         { x:  0,   y: bottomChordHeight + H },  // Коньковый узел
    leftQuarter:   { x: -L/4, y: bottomChordHeight },      // Левый промежуточный узел
    rightQuarter:  { x:  L/4, y: bottomChordHeight },      // Правый промежуточный узел
    center:        { x:  0,   y: bottomChordHeight }       // Центральный узел
};
```

### 2. Элементы фермы
1. **Нижний пояс** (Bottom Chord)
   - От: `leftBottom` → До: `rightBottom`
   - Длина: `L` (полный пролет)
   - Геометрия: стандартная балка (`createBeam`)
   
2. **Левая стропильная нога** (Left Rafter)
   - От: `leftBottom` → До: `ridge` (x=0)
   - Длина: `√((L/2)² + H²)`
   - Геометрия: **балка с торцевым срезом** (`createBeveledBeam`)
   - Угол среза: `arctan(H / (L/2))`
   
3. **Правая стропильная нога** (Right Rafter)
   - От: `ridge` (x=0) → До: `rightBottom`
   - Длина: `√((L/2)² + H²)`
   - Геометрия: **балка с торцевым срезом** (`createBeveledBeam`)
   - Угол среза: `-arctan(H / (L/2))` (зеркально)
   
4. **Центральная стойка** (King Post)
   - От: `center` → До: `ridge`
   - Длина: `H` (высота подъема кровли)
   - Геометрия: стандартная балка
   
5. **Левый раскос** (Left Diagonal Brace)
   - От: `leftQuarter` → До: `ridge`
   - Геометрия: стандартная балка
   
6. **Правый раскос** (Right Diagonal Brace)
   - От: `rightQuarter` → До: `ridge`
   - Геометрия: стандартная балка

---

## 📐 МАТЕМАТИЧЕСКАЯ МОДЕЛЬ

### Позиционирование фермы в пространстве

#### Исходные параметры:
- `height` - высота столбов (от земли до верха столба)
- `beamDimensions.height` - высота мауэрлата (продольной балки)
- `trussDimensions.height` - высота сечения элементов фермы
- `H` (roofHeight) - подъем кровли от карниза до конька

#### Формулы высот:

1. **Верхняя поверхность мауэрлата:**
   ```
   h_mauerlat_top = height + beamDimensions.height
   ```

2. **Центр нижнего пояса фермы:**
   ```
   bottomChordHeight = h_mauerlat_top + trussDimensions.height / 2
   ```
   
   **ИЛИ (упрощенная формула):**
   ```
   bottomChordHeight = height + beamDimensions.height + trussDimensions.height / 2
   ```

3. **Верхняя поверхность нижнего пояса (карниз):**
   ```
   h_eave = height + beamDimensions.height + trussDimensions.height
   ```

4. **Коньковый узел (центр сечения):**
   ```
   h_ridge_center = bottomChordHeight + H
   ```

5. **Верх конька (для кровли):**
   ```
   h_ridge_top = h_eave + H + roofLift
   где roofLift = 15мм = 0.015м
   ```

### Геометрия торцевого соединения

#### Угол наклона стропильной ноги:
```
α = arctan(H / (L/2))
где:
  H - высота подъема кровли
  L - пролет фермы
```

#### Смещение торца при срезе:
```
bevelOffset = (h / 2) * tan(α)
где:
  h - высота сечения элемента фермы
```

#### Вершины срезанного торца (правого):
```
Нижняя точка:  x = length/2 + bevelOffset, y = -h/2
Верхняя точка: x = length/2 - bevelOffset, y = +h/2
```

---

## 🎯 ТОРЦЕВОЕ СОЕДИНЕНИЕ СТРОПИЛЬНЫХ НОГ

### Особенности реализации:

1. **Точка встречи:**
   - Обе стропильные ноги сходятся в точке `(x=0, y=bottomChordHeight+H)`
   - Торцы срезаны под углом `±α` для плотного прилегания

2. **Геометрия среза:**
   - Левая нога: торец срезан под углом `+α` (против часовой стрелки)
   - Правая нога: торец срезан под углом `-α` (по часовой стрелке)
   - Срезы симметричны и точно стыкуются

3. **Преимущества:**
   - ✅ Реалистичное столярное соединение
   - ✅ Правильная передача нагрузки
   - ✅ Визуально корректное отображение
   - ✅ Автоматический расчет угла среза

---

## 🔄 СВЯЗЬ С ДРУГИМИ ЭЛЕМЕНТАМИ

### 1. С мауэрлатом:
```
Нижний пояс фермы ЛЕЖИТ на верхней поверхности мауэрлата
Центр нижнего пояса = h_mauerlat_top + trussDimensions.height/2
```

### 2. С кровлей:
```
Кровля лежит на верхней поверхности стропильных ног
h_roof_base = h_eave + roofLift
h_roof_ridge = h_eave + H + roofLift
```

### 3. Количество ферм:
```
postsAlongLength = ceil(length / postSpacing) + 1
```
Фермы располагаются над каждым столбом вдоль длины навеса.

---

## 🛠️ ФУНКЦИИ И МЕТОДЫ

### createTrusses()
**Назначение:** Создание массива ферм вдоль длины навеса  
**Входные параметры:**
- `length` - длина навеса
- `width` - ширина навеса (пролет фермы)
- `height` - высота столбов
- `roofHeight` - подъем кровли
- `woodMaterial` - материал для элементов фермы
- `postMaterial` - тип сечения столбов (для размера мауэрлата)
- `trussMaterial` - тип сечения элементов фермы

**Алгоритм:**
1. Получить размеры мауэрлата
2. Получить размеры сечения фермы
3. Вычислить `bottomChordHeight`
4. Создать параметры фермы
5. Для каждой позиции вдоль длины:
   - Построить ферму (`buildTruss`)
   - Позиционировать по оси Z
   - Добавить в группу навеса

### buildTruss()
**Назначение:** Построение одной фермы  
**Входные параметры:**
- `span` (L) - пролет
- `rise` (H) - подъем
- `sectionSize` - размеры сечения элементов
- `bottomChordHeight` - высота нижнего пояса
- `woodMaterial` - материал

**Алгоритм:**
1. Определить узлы фермы
2. Вычислить угол наклона стропил
3. Создать нижний пояс
4. Создать левую стропильную ногу с торцевым срезом
5. Создать правую стропильную ногу с торцевым срезом
6. Создать центральную стойку
7. Создать раскосы
8. Вернуть группу элементов

### createBeveledBeam()
**Назначение:** Создание балки с торцевым срезом под углом  
**Входные параметры:**
- `startNode` - начальная точка
- `endNode` - конечная точка
- `sectionSize` - размеры сечения (width, height)
- `material` - материал
- `bevelAngle` - угол среза

**Алгоритм:**
1. Вычислить длину и направление балки
2. Вычислить смещение торца: `bevelOffset = h * tan(bevelAngle) / 2`
3. Создать 8 вершин призмы:
   - 4 вершины левого торца (прямой)
   - 4 вершины правого торца (наклонный)
4. Определить 12 треугольников для 6 граней
5. Вычислить нормали для освещения
6. Создать BufferGeometry
7. Позиционировать и повернуть в пространстве

---

## 📏 ИСПОЛЬЗУЕМЫЕ РАЗМЕРЫ СЕЧЕНИЙ

### Сечение элементов фермы (trussMaterial):
- **Строганый 45×190 мм** (по умолчанию)
- Строганый 40×150 мм
- Строганый 50×200 мм
- Клееный брус 45×190 мм
- Клееный брус 50×200 мм

### Применение:
```javascript
trussDimensions = getTrussDimensions(trussMaterial);
// Возвращает: { width: 0.045, height: 0.19 } для 45×190мм
```

---

## ⚠️ ПРОБЛЕМЫ И ОГРАНИЧЕНИЯ

### Текущие ограничения:

1. **Реализован только двускатный тип**
   - Односкатный (var-1) не реализован
   - Арочный (var-3) не реализован

2. **Упрощенная решетка фермы**
   - Только один центральный раскос с каждой стороны
   - Нет возможности выбора типа решетки
   - Для больших пролетов может потребоваться усложнение

3. **Отсутствие вариантов соединений**
   - Только торцевое соединение в коньке
   - Нет накладок/болтов в узлах
   - Нет визуализации крепежа

4. **Нормали срезанных торцов**
   - Упрощенный расчет нормалей (только 8 нормалей вместо полного набора)
   - Может влиять на освещение срезанной поверхности

---

## 🎯 РЕКОМЕНДАЦИИ ПО УЛУЧШЕНИЮ

### Приоритет 1 (Критично):
1. ✅ ~~Реализовать торцевое соединение~~ - **ВЫПОЛНЕНО**
2. ⏳ Реализовать односкатную ферму (var-1)
3. ⏳ Реализовать арочную ферму (var-3)

### Приоритет 2 (Важно):
4. Улучшить расчет нормалей для срезанных торцов
5. Добавить больше типов решеток (треугольная, раскосная, полураскосная)
6. Добавить визуализацию крепежа в узлах

### Приоритет 3 (Желательно):
7. Параметрический выбор типа решетки
8. Добавить фермы Полонсо для больших пролетов
9. Оптимизация: кэширование геометрии ферм

---

## 📊 СТАТИСТИКА КОДА

### Функции для ферм:
- `createTrusses()` - 25 строк
- `buildTruss()` - 45 строк
- `createBeam()` - 25 строк
- `createBeveledBeam()` - 82 строки
- **Итого:** ~177 строк кода

### Геометрия:
- Узлов в одной ферме: 6
- Элементов в одной ферме: 6 (1 нижний пояс + 2 стропила + 1 стойка + 2 раскоса)
- Вершин в срезанной балке: 8
- Треугольников в срезанной балке: 12

---

## 🔍 ДЕТАЛИ РЕАЛИЗАЦИИ

### 1. Порядок создания элементов навеса:
```javascript
createPosts()              // Столбы
createLongitudinalBeams()  // Мауэрлаты (продольные балки)
createBeamBraces()        // Раскосы под балками (отключено)
createTrusses()           // Фермы ← ТЕКУЩИЙ ЭТАП
createRoofCovering()      // Кровельное покрытие
createGround()            // Земля
```

### 2. Система координат:
- **X**: поперек навеса (от -width/2 до +width/2)
- **Y**: высота (от 0 вверх)
- **Z**: вдоль навеса (от -length/2 до +length/2)

### 3. Центрирование:
Все элементы центрируются относительно начала координат (0, 0, 0)

---

## 🧪 ТЕСТИРОВАНИЕ

### Проверено:
- ✅ Фермы создаются в правильных позициях
- ✅ Торцевое соединение работает корректно
- ✅ Угол среза вычисляется правильно
- ✅ Кровля лежит на верхних поясах

### Требует проверки:
- ⏳ Работа при экстремальных углах (очень пологая/крутая крыша)
- ⏳ Производительность при большом количестве ферм (>50)
- ⏳ Корректность освещения на срезанных торцах

---

## 📝 ПРИМЕЧАНИЯ

1. **Торцевое соединение** реализовано через создание специальной геометрии BufferGeometry с наклонным торцом
2. **Угол среза** автоматически вычисляется на основе геометрии фермы
3. **Материал** всех элементов фермы одинаковый (woodMaterial)
4. **Тени** включены для всех элементов (`castShadow` и `receiveShadow`)

---

**Конец анализа**

