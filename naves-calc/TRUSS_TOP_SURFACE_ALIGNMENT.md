# 🏗️ СТЫКОВКА ВЕРХНИХ ГРАНЕЙ В КОНЬКЕ

**Дата:** 27 октября 2025  
**Версия:** 2.2.2  
**Статус:** ✅ Реализовано

---

## 🎯 ИЗМЕНЕНИЕ

Теперь в коньковом узле стыкуются **ВЕРХНИЕ ГРАНИ** стропильных ног, а не оси через центр сечения.

---

## 📐 КАК ЭТО РАБОТАЕТ

### ДО изменения (v2.2.1):

```
        ● ← ОСЬ в коньке (центр сечения)
       /│\
  ┌───/─│─\───┐
  │  /  │  \  │  ← верхняя грань выше оси на b/2
  │ /   │   \ │
  └/────│────\┘
  /     │     \
```

**Координата оси:**
```javascript
yTop = bottomChordHeight + H * (1 - Math.abs(2 * i / N - 1))
```

**Координата верхней грани:**
```
y_верх = yTop + b/2
```

---

### ПОСЛЕ изменения (v2.2.2):

```
       ─┬─  ← ВЕРХНЯЯ ГРАНЬ в коньке (расчетная высота)
       /│\
  ┌───/─│─\───┐
  │  /  │  \  │  ← верхняя грань ТОЧНО в расчетной точке
  │ ●   │   ● │  ← ось смещена вниз на b/2
  └/────│────\┘
  /     │     \
```

**Координата верхней грани (расчетная высота конька):**
```javascript
yTopSurface = bottomChordHeight + H * (1 - Math.abs(2 * i / N - 1))
```

**Координата оси (смещена вниз):**
```javascript
yTop = yTopSurface - b / 2
```

**Координата верхней грани бруса:**
```
y_верх = yTop + b/2 = yTopSurface  ✓
```

---

## 💡 ЗАЧЕМ ЭТО НУЖНО?

### 1. Правильная геометрия кровли

Кровля должна лежать на **верхних гранях** стропильных ног, а не на осях:

```
═══════════════════════  ← Кровля
       ─┬─              ← Верхняя грань стропил (точка стыка)
       /│\
  ┌───/─│─\───┐
  │  /  │  \  │        ← Стропильные ноги
  │ ●   │   ● │        ← Оси (внутри)
  └/────│────\┘
```

### 2. Точная высота конька

Параметр `H` (rise) теперь означает:
- ✅ Высота **верхней грани** конька над нижним поясом
- ✅ Высота кровли (там, где она реально находится)

**Было:**
- H = высота оси конька
- Верх кровли был на H + b/2

**Стало:**
- H = высота верхней грани конька
- Верх кровли точно на H ✓

### 3. Плотная стыковка в коньке

Верхние грани идеально стыкуются в одной точке:

```
           Конек
            │
      ──────┴──────  ← верхние грани встречаются
     ╱      │      ╲
    ╱   ●   │   ●   ╲  ← оси внутри
   ╱        │        ╲
```

---

## 📊 МАТЕМАТИКА

### Формулы высот для узла i:

```javascript
// Расчетная высота верхней грани (параметр H)
yTopSurface = bottomChordHeight + H * (1 - |2*i/N - 1|)

// Ось стропильной ноги (смещена вниз)
yTop = yTopSurface - b/2

// Верхняя грань бруса
y_верх = yTop + b/2 = yTopSurface  ✓

// Нижняя грань бруса
y_низ = yTop - b/2 = yTopSurface - b
```

### Пример для конька (i = N/2 = 4):

**Параметры:**
- bottomChordHeight = 3.0 м
- H = 2.4 м
- b = 0.12 м (120 мм)

**Расчет:**
```
yTopSurface = 3.0 + 2.4 * (1 - 0) = 5.4 м    (верхняя грань конька)
yTop = 5.4 - 0.06 = 5.34 м                   (ось конька)
y_верх = 5.34 + 0.06 = 5.4 м                 (верхняя грань) ✓
y_низ = 5.34 - 0.06 = 5.28 м                 (нижняя грань)
```

---

## 🎨 ВИЗУАЛИЗАЦИЯ

### Вид сбоку с размерами:

```
                    5.40 м ──┬──  ← ВЕРХНЯЯ ГРАНЬ конька
                            /│\     (расчетная высота H)
                           / │ \
                 5.34 м ──●──│──●── ← ОСИ (смещены вниз на 60 мм)
                         /   │   \
            ┌───────────/────│────\───────────┐
            │          /     │     \          │  120 мм (b)
            └─────────/──────│──────\─────────┘
                 5.28 м      │              ← нижняя грань
                     /       │       \
                    /        │        \
                   /         │         \
                  /          │          \
        ─────────●───────────●───────────●─────────
                       3.00 м               ← нижний пояс
                  (bottomChordHeight)

              ↑─────── 2.40 м ───────↑
                        (H)
```

---

## 🔧 ИЗМЕНЕНИЯ В КОДЕ

### Файл: `naves-calc/assets/js/naves-calc.bundle.js`

**Строки 2061-2073:**

**БЫЛО:**
```javascript
for (let i = 0; i <= N; i++) {
    const x = -L/2 + L * i / N;
    const yTop = bottomChordHeight + H * (1 - Math.abs(2 * i / N - 1));
    bot.push(new THREE.Vector3(x, bottomChordHeight, 0));
    top.push(new THREE.Vector3(x, yTop, 0));
}
```

**СТАЛО:**
```javascript
for (let i = 0; i <= N; i++) {
    const x = -L/2 + L * i / N;
    
    // Высота верхней грани (там, где должна быть кровля)
    const yTopSurface = bottomChordHeight + H * (1 - Math.abs(2 * i / N - 1));
    
    // Ось стропильной ноги смещена ВНИЗ на половину высоты сечения
    // чтобы верхняя грань проходила через расчетную точку
    const yTop = yTopSurface - b / 2;
    
    bot.push(new THREE.Vector3(x, bottomChordHeight, 0));
    top.push(new THREE.Vector3(x, yTop, 0));
}
```

---

## ✅ ПРЕИМУЩЕСТВА

### 1. Точная геометрия
- ✅ Параметр H означает реальную высоту верхней грани
- ✅ Кровля лежит на верхних гранях
- ✅ Высота конька соответствует параметрам

### 2. Правильная стыковка
- ✅ Верхние грани встречаются в одной точке
- ✅ Усовые подрезки работают корректно
- ✅ Нет щелей в коньковом узле

### 3. Визуальная корректность
- ✅ Ферма выглядит так, как в реальности
- ✅ Кровля на правильной высоте
- ✅ Конек в расчетной точке

### 4. Простота расчета кровли
- ✅ Высота кровли = yTopSurface
- ✅ Не нужно добавлять b/2
- ✅ Прямое соответствие параметру H

---

## 📏 ВЛИЯНИЕ НА ДРУГИЕ ЭЛЕМЕНТЫ

### Кровля:

Теперь кровля должна создаваться на высоте:
```javascript
roofHeight = yTopSurface = bottomChordHeight + H
```

**Не нужно** добавлять b/2, так как верхняя грань уже на правильной высоте.

### Раскосы:

Раскосы подключаются к **осям** стропильных ног (узлы top[i]), которые теперь смещены вниз на b/2.

Это правильно, так как раскосы в реальных конструкциях крепятся к оси стропильной ноги, а не к верхней грани.

### Нижний пояс:

Не изменился:
```javascript
bot[i] = new THREE.Vector3(x, bottomChordHeight, 0)
```

---

## 🔍 ПРОВЕРКА

### После обновления страницы проверьте:

1. ✅ **Коньковый узел:**
   - Верхние грани стропильных ног встречаются в одной точке
   - Нет щели между верхними гранями
   - Линия конька ровная

2. ✅ **Высота конька:**
   - Соответствует параметру H
   - Верхняя грань на расчетной высоте
   - Визуально правильно

3. ✅ **Усовые подрезки:**
   - Работают корректно на смещенных осях
   - Плотное прилегание в узлах
   - Симметрия сохранена

4. ✅ **Раскосы:**
   - Подключены к осям стропил
   - Визуально правильное положение
   - Нет пересечений

---

## 📊 СРАВНЕНИЕ ВЕРСИЙ

| Параметр | v2.2.1 | v2.2.2 |
|----------|--------|--------|
| Стыковка в коньке | Оси (центры) | ✅ Верхние грани |
| Ось стропильной ноги | yTop = bottomChordHeight + H | yTop = bottomChordHeight + H - b/2 |
| Верхняя грань | yTop + b/2 | ✅ bottomChordHeight + H |
| Значение H | Высота оси | ✅ Высота верхней грани |
| Высота кровли | H + b/2 | ✅ H (точно) |

---

## 🎯 РЕЗУЛЬТАТ

Теперь ферма работает **как в реальной конструкции**:

- ✅ Верхние грани стропильных ног стыкуются в коньке
- ✅ Параметр H означает реальную высоту конька
- ✅ Кровля лежит на верхних гранях стропил
- ✅ Визуально и геометрически правильно

**Профессиональная треугольная ферма готова!** 🏗️✨

---

**Конец документа**

Реализовано: 27 октября 2025  
Версия: 2.2.2  
Статус: ✅ Готово к использованию


