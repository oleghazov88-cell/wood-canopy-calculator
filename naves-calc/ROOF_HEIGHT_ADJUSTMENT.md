# 🏠 Корректировка высоты покрытия крыши

## 🎯 Описание

Система автоматической корректировки высоты покрытия крыши в зависимости от выбранного сечения фермы. Покрытие теперь располагается на правильной высоте относительно сечения фермы.

## 🔧 Технические детали

### Алгоритм расчета высоты

```javascript
// Получаем размеры сечения фермы
const trussDimensions = this.getTrussDimensions(trussMaterial);

// Кровля должна лежать на верхней части ферм
// baseHeight - это высота нижнего пояса ферм
// roofHeight - это высота подъема ферм
// Кровля должна располагаться на высоте сечения фермы от верхней части ферм
const he = height + beamDimensions.height / 2;  // Высота у карнизов (на уровне нижнего пояса ферм)
const H = roofHeight;  // Подъём на полпролёта B/2
const p = 2 * H / B;  // Уклон ската p = tan(α) = 2H/B
const hr = he + H + trussDimensions.height / 2;  // Высота конька + половина сечения фермы
```

### Функция создания покрытия

```javascript
createRoofCovering(length, width, height, roofHeight, roofingMaterial, frontExtension, backExtension, roofType, roofColor, trussMaterial) {
    // Если выбрано "Без кровли", не создаем крышу
    if (roofingMaterial === 'no-roofing') {
        return;
    }
    
    // Создание материала кровли в зависимости от выбранного материала
    const roofMaterial = this.createRoofMaterial(roofingMaterial, roofColor);

    const beamDimensions = this.getPostDimensions(this.params.postMaterial);
    
    // Параметры крыши согласно математической модели
    const B = width;  // Пролёт по поперечнику (ось x ∈ [0, B])
    const L = length + (frontExtension + backExtension);  // Длина навеса (ось y ∈ [0, L])
    
    // Получаем размеры сечения фермы
    const trussDimensions = this.getTrussDimensions(trussMaterial);
    
    // Кровля должна лежать на верхней части ферм
    // Фермы находятся на высоте: height + beamDimensions.height / 2
    // Кровля должна располагаться на высоте сечения фермы от верхней части ферм
    const he = height + beamDimensions.height / 2;  // Высота у карнизов (на уровне нижнего пояса ферм)
    const H = roofHeight;  // Подъём на полпролёта B/2
    const p = 2 * H / B;  // Уклон ската p = tan(α) = 2H/B
    const hr = he + H + trussDimensions.height / 2;  // Высота конька + половина сечения фермы
    
    // Толщина кровельного материала в зависимости от типа
    const roofThickness = this.getRoofThickness(roofingMaterial);
    
    if (roofType === 'var-2') {
        // Создаем двускатную крышу с правильной толщиной
        // Передаем высоту с учетом сечения фермы
        this.createGabledRoof(B, L, he, H, p, roofMaterial, roofThickness, trussDimensions.height);
    }
}
```

### Функция создания двускатной крыши

```javascript
createGabledRoof(width, length, baseHeight, roofHeight, slope, material, thickness, trussSectionHeight) {
    // Создаем два ската крыши
    const halfWidth = width / 2;
    // Кровля должна лежать на верхней части ферм
    // baseHeight - это высота нижнего пояса ферм
    // roofHeight - это высота подъема ферм
    // Кровля должна располагаться на высоте сечения фермы от верхней части ферм
    const ridgeHeight = baseHeight + roofHeight + trussSectionHeight / 2;
    
    // ... создание геометрии ...
    
    // Расчет высоты для каждой точки
    for (let side = 0; side < 2; side++) {
        const yOffset = side === 0 ? thickness / 2 : -thickness / 2;
        
        for (let i = 0; i <= segmentsY; i++) {
            const y = (i / segmentsY) * length - length / 2;
            for (let j = 0; j <= segmentsX / 2; j++) {
                const x = (j / (segmentsX / 2)) * halfWidth - halfWidth;
                // Кровля должна лежать на верхней части ферм
                // baseHeight - высота нижнего пояса ферм
                // slope * (halfWidth - Math.abs(x)) - высота стропильной ноги в точке x
                // trussSectionHeight / 2 - половина сечения фермы для правильного позиционирования
                const z = baseHeight + slope * (halfWidth - Math.abs(x)) + trussSectionHeight / 2 + yOffset;
                
                leftVertices.push(x, z, y);
                leftUvs.push(j / (segmentsX / 2), i / segmentsY);
            }
        }
    }
}
```

## 📊 Примеры высот для разных сечений ферм

### Ферма 45×190 мм
- **Высота сечения:** 0.19 м
- **Половина сечения:** 0.095 м
- **Дополнительная высота покрытия:** +0.095 м

### Ферма 50×200 мм
- **Высота сечения:** 0.2 м
- **Половина сечения:** 0.1 м
- **Дополнительная высота покрытия:** +0.1 м

### Ферма 60×220 мм
- **Высота сечения:** 0.22 м
- **Половина сечения:** 0.11 м
- **Дополнительная высота покрытия:** +0.11 м

## 🎮 Использование

### Для пользователей:
1. **Выберите сечение фермы** в меню "Материал ферм"
2. **Покрытие автоматически** поднимается на высоту сечения фермы
3. **Визуальная точность** - покрытие располагается на правильной высоте
4. **Реалистичность** - соответствует реальной конструкции

### Для разработчиков:
1. **Автоматический расчет** - высота покрытия рассчитывается автоматически
2. **Зависимость от материала** - высота зависит от выбранного сечения фермы
3. **Математическая точность** - используется точная формула расчета
4. **Совместимость** - работает со всеми типами кровельных материалов

## ⚡ Преимущества

### Правильное позиционирование
- **Точная высота** - покрытие располагается на правильной высоте относительно ферм
- **Зависимость от сечения** - высота автоматически адаптируется к размеру фермы
- **Визуальная точность** - 3D модель соответствует реальной конструкции
- **Профессиональный вид** - навес выглядит реалистично

### Автоматизация
- **Автоматический расчет** - не требует ручной настройки
- **Адаптивность** - высота изменяется при смене сечения фермы
- **Консистентность** - все части покрытия имеют одинаковую высоту
- **Надежность** - работает со всеми типами ферм

## 🔄 Логика работы

### 1. Получение размеров фермы
- Система получает размеры сечения фермы из `trussMaterial`
- Вычисляется половина высоты сечения фермы

### 2. Расчет базовой высоты
- `baseHeight` - высота нижнего пояса ферм
- `roofHeight` - высота подъема ферм
- `trussSectionHeight / 2` - половина сечения фермы

### 3. Создание покрытия
- Покрытие создается с учетом дополнительной высоты
- Каждая точка покрытия рассчитывается с учетом сечения фермы
- Толщина покрытия сохраняется

### 4. Позиционирование
- Левый и правый скаты создаются с одинаковой высотой
- Верхняя и нижняя поверхности покрытия учитывают толщину
- UV-координаты рассчитываются корректно

## 📐 Математическая модель

### Формула высоты покрытия:
```
z = baseHeight + slope * (halfWidth - |x|) + trussSectionHeight / 2 + yOffset
```

Где:
- `baseHeight` - высота нижнего пояса ферм
- `slope` - уклон ската (tan(α) = 2H/B)
- `halfWidth` - половина ширины навеса
- `|x|` - абсолютное значение координаты x
- `trussSectionHeight / 2` - половина сечения фермы
- `yOffset` - смещение для толщины покрытия

### Примеры расчетов:

#### Для фермы 45×190 мм:
- `trussSectionHeight = 0.19 м`
- `trussSectionHeight / 2 = 0.095 м`
- Дополнительная высота: +0.095 м

#### Для фермы 50×200 мм:
- `trussSectionHeight = 0.2 м`
- `trussSectionHeight / 2 = 0.1 м`
- Дополнительная высота: +0.1 м

## 🎯 Результат

После корректировки высоты покрытие:
- ✅ **Правильно позиционировано** на высоте сечения фермы
- ✅ **Автоматически адаптируется** к размеру сечения фермы
- ✅ **Визуально точно** соответствует реальной конструкции
- ✅ **Математически корректно** рассчитано
- ✅ **Профессионально выглядит** в 3D модели

**Покрытие теперь располагается на правильной высоте в зависимости от выбранного сечения фермы!** 🎯
