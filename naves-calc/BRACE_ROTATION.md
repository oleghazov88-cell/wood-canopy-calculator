# 🔄 Поворот раскосов по вертикали

Калькулятор навесов поддерживает поворот GLB моделей раскосов на 90 градусов по вертикали для правильного размещения в 3D модели.

## 🎯 Функциональность

### 1. **Поворот раскосов**
- **Вертикальный поворот** - раскосы поворачиваются на 90 градусов по оси Y
- **Правильное размещение** - раскосы корректно ориентированы в 3D пространстве
- **Симметричность** - левый и правый раскосы поворачиваются одинаково

### 2. **Поддерживаемые типы раскосов**
- **r1** - Раскос типа 1
- **r2** - Раскос типа 2  
- **r3** - Раскос типа 3
- **r4** - Раскос типа 4

## 🔧 Технические детали

### Алгоритм поворота

```javascript
// Получаем размеры сечения столба
const postDimensions = this.getPostDimensions(postMaterial);

// Создаем левый раскос
const leftBrace = glbModel.clone();
leftBrace.position.set(-width/2 - postDimensions.width/2, beamHeight - 0.5, z); // На расстоянии 1/2 сечения от оси столба
// Поворачиваем раскос на 90 градусов по вертикали (оси Y)
leftBrace.rotation.y = Math.PI / 2;

// Создаем правый раскос
const rightBrace = glbModel.clone();
rightBrace.position.set(width/2 + postDimensions.width/2, beamHeight - 0.5, z); // На расстоянии 1/2 сечения от оси столба
// Поворачиваем раскос на 90 градусов по вертикали (оси Y)
rightBrace.rotation.y = Math.PI / 2;
```

### Функция создания раскосов

```javascript
async createBeamBraces(length, width, height, woodMaterial, frontExtension, backExtension, postMaterial, braceType) {
    // Получаем размеры сечения столба для правильного позиционирования раскосов
    const postDimensions = this.getPostDimensions(postMaterial);
    
    // Загружаем GLB модель раскоса
    const glbModel = await this.loadBraceGLB(braceType);
    
    for (let i = 0; i < postsAlongLength; i++) {
        const z = -length/2 + (i * length / (postsAlongLength - 1));
        
        if (glbModel) {
            // Левый раскос с поворотом
            const leftBrace = glbModel.clone();
            leftBrace.position.set(-width/2 - postDimensions.width/2, beamHeight - 0.5, z); // На расстоянии 1/2 сечения от оси столба
            leftBrace.rotation.y = Math.PI / 2; // Поворот на 90° по вертикали
            
            // Правый раскос с поворотом
            const rightBrace = glbModel.clone();
            rightBrace.position.set(width/2 + postDimensions.width/2, beamHeight - 0.5, z); // На расстоянии 1/2 сечения от оси столба
            rightBrace.rotation.y = Math.PI / 2; // Поворот на 90° по вертикали
        }
    }
}
```

## 📊 Примеры позиционирования

### До изменений
- **Ориентация:** Горизонтальная (по умолчанию)
- **Позиция:** На середине высоты столбов (beamHeight/2)
- **Проблема:** Раскосы неправильно ориентированы и расположены не под балками

### После изменений
- **Ориентация:** Вертикальная (поворот на 90° по оси Y)
- **Позиция:** Под балками (beamHeight - 0.5)
- **Смещение:** На расстоянии 1/2 сечения столба от оси столба
- **Результат:** Раскосы корректно размещены под балками с правильной ориентацией и позиционированием

### Примеры для разных размеров столбов

#### Столб 150×150 мм
- **Ось столба:** -width/2 и width/2
- **Левый раскос:** -width/2 - 0.075 м (75 мм от оси)
- **Правый раскос:** width/2 + 0.075 м (75 мм от оси)

#### Столб 200×200 мм
- **Ось столба:** -width/2 и width/2
- **Левый раскос:** -width/2 - 0.1 м (100 мм от оси)
- **Правый раскос:** width/2 + 0.1 м (100 мм от оси)

#### Столб 100×100 мм
- **Ось столба:** -width/2 и width/2
- **Левый раскос:** -width/2 - 0.05 м (50 мм от оси)
- **Правый раскос:** width/2 + 0.05 м (50 мм от оси)

## 🎮 Использование

### Для пользователей:
1. **Выберите тип раскоса** в меню "Раскосы" (r1, r2, r3, r4)
2. **Наблюдайте** как раскосы автоматически поворачиваются на 90 градусов
3. **Проверьте** правильность размещения в 3D модели

### Для разработчиков:
1. **GLB модели** раскосов должны быть созданы в горизонтальной ориентации
2. **Поворот** применяется автоматически при создании раскосов
3. **Кэширование** GLB моделей работает независимо от поворота

## ⚡ Преимущества

### Правильная ориентация и позиционирование
- **Корректное размещение** - раскосы правильно ориентированы в 3D пространстве
- **Позиция под балками** - раскосы расположены под балками, как в реальной конструкции
- **Точное смещение** - раскосы находятся на расстоянии 1/2 сечения столба от оси столба
- **Визуальная точность** - 3D модель соответствует реальной конструкции
- **Профессиональный вид** - навес выглядит реалистично

### Автоматизация
- **Автоматический поворот** - не требует ручной настройки
- **Симметричность** - левый и правый раскосы поворачиваются одинаково
- **Консистентность** - все раскосы имеют одинаковую ориентацию

## 🔄 Процесс работы

### 1. **Загрузка GLB модели**
- Загружается GLB файл раскоса из папки `../raskos/`
- Модель кэшируется для повторного использования

### 2. **Создание раскосов**
- GLB модель клонируется для каждого раскоса
- Устанавливается позиция в 3D пространстве

### 3. **Применение поворота**
- Применяется поворот на 90 градусов по оси Y
- `rotation.y = Math.PI / 2`

### 4. **Добавление в сцену**
- Раскос добавляется в 3D сцену
- Настраиваются тени и освещение

## 📁 Структура файлов

```
raskos/
├── r1.glb          # Раскос типа 1
├── r2.glb          # Раскос типа 2
├── r3.glb          # Раскос типа 3
└── r4.glb          # Раскос типа 4
```

## 🎯 Результат

После применения поворота и изменения позиции раскосы:
- ✅ **Правильно ориентированы** в 3D пространстве (поворот на 90° по оси Y)
- ✅ **Корректно размещены** под балками (позиция beamHeight - 0.5)
- ✅ **Точно смещены** на расстояние 1/2 сечения столба от оси столба
- ✅ **Симметрично расположены** слева и справа
- ✅ **Визуально точны** в соответствии с реальной конструкцией
- ✅ **Расположены под балками** как в реальном навесе

**Раскосы теперь поворачиваются на 90 градусов по вертикали и располагаются под балками на правильном расстоянии от оси столбов!** 🎯
