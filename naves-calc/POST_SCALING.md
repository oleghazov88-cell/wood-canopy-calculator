# 🔧 Масштабирование GLB моделей столбов

## 📋 Обзор

Калькулятор навесов теперь поддерживает автоматическое масштабирование GLB моделей столбов в соответствии с выбранным материалом через меню "Материал столбов".

## 🎯 Функциональность

### 1. **Автоматическое масштабирование**
- **Адаптация к материалу** - GLB модели автоматически масштабируются под выбранный материал
- **Регулирование высоты** - высота столба изменяется через бегунок "Высота столбов"
- **Базовые размеры** - за основу взят размер 150x150 мм и высота 3 метра

### 2. **Поддерживаемые материалы**
- **Клееный брус 100×100 мм** - масштаб 0.67x0.67
- **Клееный брус 120×120 мм** - масштаб 0.8x0.8
- **Клееный брус 140×140 мм** - масштаб 0.93x0.93
- **Клееный брус 150×150 мм** - масштаб 1.0x1.0 (базовый)
- **Клееный брус 180×180 мм** - масштаб 1.2x1.2
- **Клееный брус 200×200 мм** - масштаб 1.33x1.33
- **Строганый брус 100×100 мм** - масштаб 0.67x0.67
- **Строганый брус 120×120 мм** - масштаб 0.8x0.8
- **Строганый брус 140×140 мм** - масштаб 0.93x0.93
- **Строганый брус 150×150 мм** - масштаб 1.0x1.0 (базовый)
- **Строганый брус 180×180 мм** - масштаб 1.2x1.2
- **Строганый брус 190×190 мм** - масштаб 1.27x1.27

## 🔧 Технические детали

### Алгоритм масштабирования

```javascript
// Получаем размеры материала столба
const postDimensions = this.getPostDimensions(postMaterial);

// Вычисляем коэффициенты масштабирования
const scaleX = postDimensions.width / 0.15; // Базовый размер 150x150
const scaleZ = postDimensions.height / 0.15;
const scaleY = height / 3.0; // Базовая высота GLB модели 3 метра

// Применяем масштабирование к GLB модели
leftPost.scale.set(scaleX, scaleY, scaleZ); // Масштабируем по всем осям
```

### Функция получения размеров

```javascript
getPostDimensions(postMaterial) {
    const match = postMaterial.match(/(\d+)x(\d+)/);
    if (match) {
        const width = parseInt(match[1]) / 1000;  // Конвертация в метры
        const height = parseInt(match[2]) / 1000;
        return { width, height };
    }
    return { width: 0.15, height: 0.15 }; // По умолчанию 150x150
}
```

### Создание столбов с масштабированием

```javascript
// Левый столб
if (glbModel) {
    // Используем GLB модель с масштабированием по материалу
    const leftPost = glbModel.clone();
    leftPost.position.set(-width/2, height/2, z);
    
    // Масштабируем GLB модель в соответствии с материалом столба и высотой
    const scaleX = postDimensions.width / 0.15; // Базовый размер 150x150
    const scaleZ = postDimensions.height / 0.15;
    const scaleY = height / 3.0; // Базовая высота GLB модели 3 метра
    leftPost.scale.set(scaleX, scaleY, scaleZ); // Масштабируем по всем осям
    
    leftPost.castShadow = true;
    leftPost.receiveShadow = true;
    this.canopyGroup.add(leftPost);
}
```

## 📊 Примеры масштабирования

### Материал: Клееный брус 100×100 мм, Высота: 2.5 м
- **Исходный размер GLB:** 150×150×3000 мм
- **Целевой размер:** 100×100×2500 мм
- **Коэффициент масштабирования:** 0.67×0.67×0.83
- **Результат:** Столб становится на 33% меньше по сечению и на 17% ниже

### Материал: Клееный брус 200×200 мм, Высота: 4.0 м
- **Исходный размер GLB:** 150×150×3000 мм
- **Целевой размер:** 200×200×4000 мм
- **Коэффициент масштабирования:** 1.33×1.33×1.33
- **Результат:** Столб становится на 33% больше по сечению и на 33% выше

### Материал: Клееный брус 150×150 мм, Высота: 3.0 м
- **Исходный размер GLB:** 150×150×3000 мм
- **Целевой размер:** 150×150×3000 мм
- **Коэффициент масштабирования:** 1.0×1.0×1.0
- **Результат:** Столб остается без изменений (базовый размер)

## 🎮 Использование

### Для пользователей:
1. **Выберите тип столба** в меню "Столбы" (var-2, var-3, var-4, var-5, var-6, var-7)
2. **Выберите материал** в меню "Материал столбов"
3. **Регулируйте высоту** через бегунок "Высота столбов"
4. **Наблюдайте** как GLB модель автоматически масштабируется под выбранный материал и высоту
5. **Сечение и высота** изменяются в соответствии с настройками

### Для разработчиков:
1. **GLB модели** должны быть созданы с базовым размером 150×150 мм
2. **Масштабирование** применяется автоматически при создании столбов
3. **Кэширование** GLB моделей работает независимо от масштабирования

## 🔄 Процесс работы

### 1. **Выбор материала**
- Пользователь выбирает материал столба в селекте
- Срабатывает обработчик события `change`

### 2. **Получение размеров**
- Функция `getPostDimensions()` парсит размеры из названия материала
- Возвращаются размеры в метрах

### 3. **Вычисление масштаба**
- Вычисляются коэффициенты масштабирования относительно базовых размеров 150×150×3000
- `scaleX = postDimensions.width / 0.15`
- `scaleZ = postDimensions.height / 0.15`
- `scaleY = height / 3.0`

### 4. **Применение масштабирования**
- GLB модель клонируется для каждого столба
- Применяется масштабирование `scale.set(scaleX, scaleY, scaleZ)`
- Столб добавляется в 3D сцену

## ⚡ Преимущества

### Гибкость
- **Один GLB файл** для всех размеров материала
- **Автоматическое масштабирование** без создания множества файлов
- **Легкость добавления** новых материалов

### Производительность
- **Кэширование GLB** моделей работает эффективно
- **Масштабирование** применяется только при создании столбов
- **Минимальное потребление памяти**

### Точность
- **Точные размеры** в соответствии с выбранным материалом
- **Сохранение пропорций** GLB модели
- **Правильное позиционирование** в 3D сцене

## 🐛 Обработка ошибок

### Сценарии ошибок:
1. **Некорректный материал** - используется базовый размер 150×150
2. **Ошибка парсинга** - используется базовый размер 150×150
3. **Отсутствие GLB модели** - используется стандартная геометрия

### Fallback механизм:
```javascript
return { width: 0.15, height: 0.15 }; // По умолчанию 150x150
```

## 🔮 Планы развития

### Краткосрочные:
- [ ] **Валидация размеров** GLB моделей при загрузке
- [ ] **Предпросмотр масштабирования** в интерфейсе
- [ ] **Сохранение настроек** масштабирования

### Долгосрочные:
- [ ] **Анимированное масштабирование** при смене материала
- [ ] **Настраиваемые базовые размеры** для разных GLB моделей
- [ ] **Автоматическая оптимизация** GLB моделей под разные размеры

## 📚 Дополнительные ресурсы

- [Three.js Object3D.scale](https://threejs.org/docs/#api/en/core/Object3D.scale)
- [GLB File Format](https://www.khronos.org/gltf/)
- [3D Model Scaling Best Practices](https://threejs.org/docs/#manual/en/introduction/Performance-tips)

---

**Версия:** 1.0  
**Дата:** 2024  
**Автор:** AI Assistant  
**Статус:** ✅ Готово к продакшену
