# 🚀 Как запустить через Live Server

## ✅ Способ 1: Правильная структура (РЕКОМЕНДУЕТСЯ)

### Структура проекта:
```
wood_canopy_calc/          ← Откройте ЭТУ папку в VS Code
├── naves-calc/
│   ├── index.html         ← Запускайте ЭТОТ файл
│   ├── assets/
│   ├── api/
│   └── ...
```

### Шаги:

1. **Откройте VS Code**
   - File → Open Folder
   - Выберите папку `wood_canopy_calc`

2. **Установите Live Server** (если еще не установлен)
   - Extensions → Поиск "Live Server"
   - Установите от Ritwick Dey

3. **Запустите**
   - Откройте `naves-calc/index.html`
   - Правый клик → "Open with Live Server"
   - ИЛИ нажмите кнопку "Go Live" внизу справа

4. **Откроется в браузере:**
   ```
   http://127.0.0.1:5500/naves-calc/index.html
   ```

✅ **Это должно работать!** Все пути типа `/naves-calc/assets/...` будут правильно резолвиться.

---

## 🔧 Способ 2: Если открыли только папку naves-calc

Если вы открыли в VS Code только папку `naves-calc` (а не `wood_canopy_calc`), то пути `/naves-calc/...` НЕ будут работать!

### Решение А: Используйте относительные пути

Замените в `index.html`:
- `/naves-calc/assets/...` → `assets/...`
- `/naves-calc/api/...` → `api/...`

### Решение Б: Настройте Live Server

Создайте `.vscode/settings.json` в папке `naves-calc`:

```json
{
  "liveServer.settings.root": "/",
  "liveServer.settings.mount": [
    ["/naves-calc", "."]
  ]
}
```

---

## 🌐 Альтернативные способы запуска

### Python (встроенный в Windows)
```bash
cd C:\Users\OLEG\Desktop\AZ\wood_canopy_calc
python -m http.server 8000
```
Откройте: `http://localhost:8000/naves-calc/`

### Node.js (если установлен)
```bash
cd C:\Users\OLEG\Desktop\AZ\wood_canopy_calc
npx http-server -p 8000
```
Откройте: `http://localhost:8000/naves-calc/`

### PHP (если установлен)
```bash
cd C:\Users\OLEG\Desktop\AZ\wood_canopy_calc
php -S localhost:8000
```
Откройте: `http://localhost:8000/naves-calc/`

---

## 🐛 Возможные проблемы и решения

### Проблема 1: 404 на скриптах/стилях
**Причина:** Неправильная корневая папка

**Решение:** 
- Убедитесь, что открыли `wood_canopy_calc`, а не `naves-calc`
- ИЛИ измените пути на относительные

### Проблема 2: CORS ошибки
**Причина:** Браузер блокирует загрузку файлов

**Решение:**
- Используйте Live Server (автоматически решает CORS)
- НЕ открывайте файл напрямую через `file://`

### Проблема 3: 3D не загружается
**Причина:** Three.js не загрузился

**Решение:**
- Проверьте консоль (F12)
- Убедитесь, что файлы в `assets/libs/three/` существуют
- Очистите кэш (Ctrl+Shift+R)

### Проблема 4: MVP не инициализируется
**Причина:** Скрипты MVP не загрузились

**Решение:**
- Проверьте консоль на ошибки
- Убедитесь, что все файлы в `assets/js/mvp/` существуют
- Проверьте порядок подключения скриптов

---

## ✅ Проверка работоспособности

После запуска Live Server, откройте консоль (F12) и проверьте:

```javascript
// 1. Должны увидеть в консоли:
// ✅ MVP Architecture активирована
// ✅ Обертка совместимости window.NavesCalc создана

// 2. Проверьте доступность API:
console.log(window.CanopyApp);
// Должен вернуть объект с presenter, model, view, renderer

// 3. Проверьте 3D:
console.log(window.CanopyApp.renderer.scene.children.length);
// Должно быть > 10 (много объектов в сцене)
```

---

## 🎯 Рекомендация

**Лучший способ для разработки:**

1. Откройте VS Code в папке `wood_canopy_calc`
2. Установите Live Server
3. Откройте `naves-calc/index.html`
4. Нажмите "Go Live"

**Готово!** ✨

---

## 📱 Для мобильной разработки

Live Server позволяет открывать на телефоне:

1. Запустите Live Server
2. В VS Code снизу увидите IP адрес, например:
   ```
   http://192.168.1.100:5500/naves-calc/
   ```
3. Откройте этот адрес на телефоне (должны быть в одной сети)

---

## 🔥 Горячие клавиши Live Server

- `Alt + L, Alt + O` - Открыть с Live Server
- `Alt + L, Alt + C` - Остановить Live Server
- Или используйте кнопку "Go Live" внизу справа

---

**Удачной разработки!** 🚀✨

