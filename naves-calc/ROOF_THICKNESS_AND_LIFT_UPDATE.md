# 📏 Обновление толщины покрытия и подъем на 15 мм

## 🎯 Описание

Восстановление оригинальных толщин для каждого типа кровельного материала и добавление подъема покрытия на 15 мм. Это обеспечивает реалистичные толщины материалов и правильное позиционирование покрытия.

## 🔧 Технические детали

### Функция получения толщины кровельного материала

```javascript
getRoofThickness(roofingMaterial) {
    switch(roofingMaterial) {
        case 'metal-grandline':
            return 0.00045; // 0.45мм
        case 'profiled-gl35r':
            return 0.0005;  // 0.5мм
        case 'shinglas-sonata':
            return 0.003;   // 3мм (многослойная структура)
        case 'polycarbonate-8mm':
            return 0.008;   // 8мм
        default:
            return 0.001;   // 1мм по умолчанию
    }
}
```

### Функция создания покрытия с подъемом

```javascript
createRoofCovering(length, width, height, roofHeight, roofingMaterial, frontExtension, backExtension, roofType, roofColor, trussMaterial) {
    // ... существующий код ...
    
    // Кровля должна располагаться на высоте сечения фермы от верхней части ферм + подъем на 15 мм
    const he = height + beamDimensions.height / 2;  // Высота у карнизов (на уровне нижнего пояса ферм)
    const H = roofHeight;  // Подъём на полпролёта B/2
    const p = 2 * H / B;  // Уклон ската p = tan(α) = 2H/B
    const roofLift = 0.015;  // Подъем покрытия на 15 мм
    const hr = he + H + trussDimensions.height / 2 + roofLift;  // Высота конька + половина сечения фермы + подъем
    
    // ... остальной код ...
    
    if (roofType === 'var-2') {
        // Создаем двускатную крышу с правильной толщиной
        // Передаем высоту с учетом сечения фермы и подъема на 15 мм
        this.createGabledRoof(B, L, he, H, p, roofMaterial, roofThickness, trussDimensions.height, roofLift);
    }
}
```

### Функция создания двускатной крыши с подъемом

```javascript
createGabledRoof(width, length, baseHeight, roofHeight, slope, material, thickness, trussSectionHeight, roofLift) {
    // Создаем два ската крыши
    const halfWidth = width / 2;
    // Кровля должна лежать на верхней части ферм
    // baseHeight - это высота нижнего пояса ферм
    // roofHeight - это высота подъема ферм
    // Кровля должна располагаться на высоте сечения фермы от верхней части ферм + подъем на 15 мм
    const ridgeHeight = baseHeight + roofHeight + trussSectionHeight / 2 + roofLift;
    
    // ... создание геометрии ...
    
    // Расчет высоты для каждой точки
    for (let side = 0; side < 2; side++) {
        const yOffset = side === 0 ? thickness / 2 : -thickness / 2;
        
        for (let i = 0; i <= segmentsY; i++) {
            const y = (i / segmentsY) * length - length / 2;
            for (let j = 0; j <= segmentsX / 2; j++) {
                const x = (j / (segmentsX / 2)) * halfWidth - halfWidth;
                // Кровля должна лежать на верхней части ферм
                // baseHeight - высота нижнего пояса ферм
                // slope * (halfWidth - Math.abs(x)) - высота стропильной ноги в точке x
                // trussSectionHeight / 2 - половина сечения фермы для правильного позиционирования
                // roofLift - подъем покрытия на 15 мм
                const z = baseHeight + slope * (halfWidth - Math.abs(x)) + trussSectionHeight / 2 + roofLift + yOffset;
                
                leftVertices.push(x, z, y);
                leftUvs.push(j / (segmentsX / 2), i / segmentsY);
            }
        }
    }
}
```

## 📊 Толщины материалов

| Материал | Толщина | Описание |
|----------|---------|----------|
| metal-grandline | 0.45 мм | Металлическая кровля |
| profiled-gl35r | 0.5 мм | Профнастил |
| shinglas-sonata | 3 мм | Многослойная структура |
| polycarbonate-8mm | 8 мм | Поликарбонат |
| default | 1 мм | По умолчанию |

## 🎯 Подъем покрытия

### Параметры подъема:
- **Подъем:** 15 мм (0.015 м)
- **Применение:** Ко всем типам кровельных материалов
- **Позиционирование:** Над верхней частью ферм

### Формула высоты покрытия:
```
z = baseHeight + slope * (halfWidth - |x|) + trussSectionHeight / 2 + roofLift + yOffset
```

Где:
- `baseHeight` - высота нижнего пояса ферм
- `slope` - уклон ската (tan(α) = 2H/B)
- `halfWidth` - половина ширины навеса
- `|x|` - абсолютное значение координаты x
- `trussSectionHeight / 2` - половина сечения фермы
- `roofLift` - подъем покрытия на 15 мм (0.015 м)
- `yOffset` - смещение для толщины покрытия

## ⚡ Преимущества

### Реалистичные толщины
- **Точные размеры** - каждый материал имеет реалистичную толщину
- **Визуальная точность** - покрытие выглядит как в реальности
- **Профессиональный вид** - навес соответствует реальным конструкциям
- **Разнообразие** - разные материалы имеют разные толщины

### Правильное позиционирование
- **Подъем на 15 мм** - покрытие располагается на правильной высоте
- **Визуальная точность** - 3D модель соответствует реальной конструкции
- **Профессиональный вид** - навес выглядит реалистично
- **Совместимость** - работает со всеми типами кровельных материалов

## 🎮 Использование

### Для пользователей:
1. **Выберите тип кровельного материала** - каждый имеет свою толщину
2. **Покрытие автоматически поднимается** на 15 мм над фермами
3. **Визуальная точность** - покрытие выглядит реалистично
4. **Профессиональный вид** - навес соответствует реальным конструкциям

### Для разработчиков:
1. **Реалистичные толщины** - каждый материал имеет точную толщину
2. **Автоматический подъем** - покрытие поднимается на 15 мм
3. **Математическая точность** - используется точная формула расчета
4. **Совместимость** - работает со всеми типами материалов

## 🔄 Логика работы

### 1. Получение толщины материала
- Система получает толщину из `getRoofThickness()`
- Каждый материал имеет свою реалистичную толщину

### 2. Расчет подъема
- Добавляется подъем на 15 мм (0.015 м)
- Подъем применяется ко всем типам материалов

### 3. Создание покрытия
- Покрытие создается с учетом толщины материала и подъема
- Верхняя и нижняя поверхности покрытия учитывают толщину
- UV-координаты рассчитываются корректно

### 4. Позиционирование
- Покрытие позиционируется с учетом подъема на 15 мм
- Толщина влияет на смещение верхней и нижней поверхностей
- Все расчеты основаны на реалистичных толщинах

## 📐 Математическая модель

### Формула с подъемом и реалистичными толщинами:
```
yOffset = side === 0 ? thickness / 2 : -thickness / 2
z = baseHeight + slope * (halfWidth - |x|) + trussSectionHeight / 2 + roofLift + yOffset
```

Где:
- `thickness` - реалистичная толщина материала (0.00045-0.008 м)
- `roofLift = 0.015` м (15 мм) - подъем покрытия
- `yOffset` - смещение для верхней/нижней поверхности
- `side === 0` - верхняя поверхность (+thickness/2)
- `side === 1` - нижняя поверхность (-thickness/2)

### Примеры расчетов:

#### Для metal-grandline (0.45 мм):
- `thickness = 0.00045 м`
- `thickness / 2 = 0.000225 м`
- `roofLift = 0.015 м`
- Верхняя поверхность: +0.000225 м
- Нижняя поверхность: -0.000225 м

#### Для polycarbonate-8mm (8 мм):
- `thickness = 0.008 м`
- `thickness / 2 = 0.004 м`
- `roofLift = 0.015 м`
- Верхняя поверхность: +0.004 м
- Нижняя поверхность: -0.004 м

## 🎯 Результат

После изменений покрытие:
- ✅ **Имеет реалистичные толщины** для каждого типа материала
- ✅ **Поднимается на 15 мм** над верхней частью ферм
- ✅ **Визуально точно** соответствует реальной конструкции
- ✅ **Профессионально выглядит** - навес выглядит реалистично
- ✅ **Математически корректно** рассчитано

**Покрытие теперь имеет реалистичные толщины материалов и поднимается на 15 мм над фермами!** 🎯
