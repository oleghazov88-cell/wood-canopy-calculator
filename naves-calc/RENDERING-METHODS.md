# 🎨 Методы отрисовки элементов в Three.js

## Обзор методов создания 3D объектов

В проекте калькулятора навесов используются **два разных метода** создания 3D элементов:

---

## 📦 1. Программное рисование (Столбы)

### Описание
**Столбы рисуются вручную программно** с использованием встроенных примитивов Three.js.

### Принцип
- ✅ Геометрия создается **программно в коде**
- ✅ Используются **встроенные примитивы** Three.js
- ❌ **Нет импорта** внешних файлов
- ❌ **Нет загрузчиков** (GLTFLoader)

### Код
```javascript
// Создаем геометрию программно (без файлов)
const postGeometry = new THREE.BoxGeometry(
    0.15,  // Ширина
    3.0,   // Высота
    0.15   // Глубина
);

// Создаем материал
const woodMaterial = new THREE.MeshStandardMaterial({
    color: 0x8B4513,
    roughness: 0.8,
    metalness: 0.2
});

// Создаем меш (3D объект)
const post = new THREE.Mesh(postGeometry, woodMaterial);

// Позиционируем в сцене
post.position.set(-3.0, 1.5, 0);
post.castShadow = true;
post.receiveShadow = true;

// Добавляем в сцену
this.canopyGroup.add(post);
```

### Преимущества
- ⚡ **Мгновенное создание** (нет загрузки)
- 💾 **Минимум памяти** (<100KB)
- 🚀 **Высокая производительность**
- 🔧 **Простота кода**
- ✅ **Нет зависимости от файлов**

### Недостатки
- ❌ Простая геометрия (только примитивы)
- ❌ Нет детализации модели
- ❌ Нельзя загрузить из 3D редактора

### Используется для
- ✅ **Столбы** - простые параллелепипеды
- ✅ **Балки** - продольные и поперечные
- ✅ **Кровля** - плоскости

---

## 📥 2. Импорт GLB моделей (Раскосы)

### Описание
**Раскосы импортируются из внешних GLB файлов** с использованием GLTFLoader.

### Принцип
- ✅ Модели создаются в **3D редакторе** (Blender, SketchUp)
- ✅ Экспортируются в формат **GLB**
- ✅ **Загружаются** в Three.js через GLTFLoader
- ✅ Можно использовать **сложную геометрию**

### Код
```javascript
async loadBraceGLB(braceType) {
    // Путь к файлу GLB модели
    const glbFile = `../raskos/r${braceType.replace('var-', '')}.glb`;
    
    return new Promise((resolve, reject) => {
        // Создаем загрузчик GLTF/GLB
        const loader = new THREE.GLTFLoader();
        
        // Загружаем внешний файл
        loader.load(
            glbFile,
            (gltf) => {
                // Модель успешно загружена
                const model = gltf.scene;
                
                // Настройка модели
                model.scale.set(1, 1, 1);
                model.castShadow = true;
                model.receiveShadow = true;
                
                resolve(model);
            },
            (progress) => {
                // Прогресс загрузки
                console.log('Загрузка:', (progress.loaded / progress.total * 100) + '%');
            },
            (error) => {
                // Ошибка загрузки
                console.error('Ошибка загрузки:', error);
                reject(error);
            }
        );
    });
}

// Использование
const braceModel = await this.loadBraceGLB('var-2');
if (braceModel) {
    const brace = braceModel.clone();
    brace.position.set(x, y, z);
    brace.rotation.z = Math.PI / 4;
    this.canopyGroup.add(brace);
}
```

### Преимущества
- ✅ **Детальные модели** любой сложности
- ✅ Создание в **3D редакторе**
- ✅ **Реалистичный вид**
- ✅ Возможность **текстур и материалов**
- ✅ **Анимации** (если есть в модели)

### Недостатки
- ❌ Требуется **загрузка файлов** (200-500ms)
- ❌ Больше **использование памяти** (2-5MB)
- ❌ **Сложность** кода (обработка загрузки)
- ❌ Зависимость от **внешних файлов**
- ❌ Нужен **GLTFLoader**

### Используется для
- ✅ **Раскосы** - сложные декоративные элементы
- ✅ Элементы со **сложной геометрией**
- ✅ Элементы, созданные в **3D редакторах**

---

## 📊 Сравнительная таблица

| Характеристика | Программное рисование | Импорт GLB |
|----------------|----------------------|------------|
| **Используется для** | Столбы, балки, кровля | Раскосы |
| **Метод создания** | `THREE.BoxGeometry()` | `GLTFLoader.load()` |
| **Время создания** | <10ms | 200-500ms |
| **Память** | <100KB | 2-5MB |
| **Сложность геометрии** | Простая (примитивы) | Любая |
| **Зависимость от файлов** | Нет | Да |
| **Детализация** | Низкая | Высокая |
| **Производительность** | Очень высокая | Средняя |
| **Редактирование** | В коде | В 3D редакторе |

---

## 🎯 Когда что использовать?

### Используйте программное рисование (BoxGeometry):
- ✅ Простые геометрические формы
- ✅ Когда важна **производительность**
- ✅ Элементы, меняющие размер динамически
- ✅ Массовые элементы (много копий)
- ✅ Когда **не нужна детализация**

**Примеры:** столбы, балки, плоские кровли, основания

### Используйте импорт GLB:
- ✅ Сложная геометрия
- ✅ Декоративные элементы
- ✅ Модели из 3D редакторов
- ✅ Когда нужна **детализация**
- ✅ Уникальные элементы

**Примеры:** раскосы, резные элементы, декор, сложные узлы

---

## 💡 Примеры в проекте

### Столбы (Программное рисование)
```javascript
// Рисуем вручную в коде
const postGeometry = new THREE.BoxGeometry(0.15, 3.0, 0.15);
const post = new THREE.Mesh(postGeometry, woodMaterial);
```

**Почему:** 
- Простая форма (параллелепипед)
- Много столбов (6-20 штук)
- Размеры меняются динамически
- Не нужна детализация

### Раскосы (Импорт GLB)
```javascript
// Загружаем из файла
const braceModel = await this.loadBraceGLB('var-2');
const brace = braceModel.clone();
```

**Почему:**
- Сложная декоративная форма
- Созданы дизайнером в 3D редакторе
- Нужна детализация и реализм
- Небольшое количество (4-12 штук)

---

## 🔧 Технические детали

### THREE.BoxGeometry (Программное рисование)

**Что это:**
- Встроенный примитив Three.js
- Создает параллелепипед (прямоугольный ящик)
- Генерирует вершины и грани автоматически

**Параметры:**
```javascript
new THREE.BoxGeometry(width, height, depth, widthSegments, heightSegments, depthSegments)
```

**Пример:**
```javascript
// Простой куб 1×1×1
const cubeGeo = new THREE.BoxGeometry(1, 1, 1);

// Столб 150×3000×150 мм
const postGeo = new THREE.BoxGeometry(0.15, 3.0, 0.15);
```

### GLTFLoader (Импорт моделей)

**Что это:**
- Загрузчик для формата GLTF/GLB
- Поддерживает сложные модели
- Асинхронная загрузка

**Пример:**
```javascript
const loader = new THREE.GLTFLoader();
loader.load('model.glb', (gltf) => {
    scene.add(gltf.scene);
});
```

---

## 📚 Дополнительные ресурсы

### Документация Three.js
- [BoxGeometry](https://threejs.org/docs/#api/en/geometries/BoxGeometry)
- [GLTFLoader](https://threejs.org/docs/#examples/en/loaders/GLTFLoader)
- [Mesh](https://threejs.org/docs/#api/en/objects/Mesh)

### Примеры
- [Three.js Examples - Geometries](https://threejs.org/examples/#webgl_geometries)
- [Three.js Examples - GLTF Loader](https://threejs.org/examples/#webgl_loader_gltf)

---

## ✅ Выводы

1. **Столбы** = Программное рисование (THREE.BoxGeometry)
   - Быстро, просто, эффективно
   - Нет файлов, нет загрузки

2. **Раскосы** = Импорт GLB (GLTFLoader)
   - Детально, красиво, реалистично
   - Требует файлы, требует время

3. **Выбор метода** зависит от:
   - Сложности геометрии
   - Требований к производительности
   - Необходимости детализации
   - Количества элементов

---

**Дата:** 13.10.2025  
**Версия:** 2.0.1  
**Автор:** Wood Canopy Calc Team

